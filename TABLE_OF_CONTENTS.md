# CUDA Hammer: A Tensor Compiler in C++ -- Table of Contents (planning document)

Subtitle: From a Minimal IR to Fused, Autotuned Kernels

This is the full planned outline. Check this file first before starting any new chapter or appendix. Mirrors the sibling books' own build-verify-lock discipline (see getting-started.md for this book's own specific honesty discipline around the toolchain actually available while writing it).

## Part 0 -- Why Compile Tensor Programs At All
1. The Cost of Eager Execution
2. What a Compiler Pipeline Looks Like
3. A Tour of Real ML Compilers: XLA, TVM, and Triton

## Part 1 -- CUDA Hammer's IR: A Minimal Tensor Graph From Scratch
4. Representing Tensors and Operations as a Graph
5. Building the Graph: CUDA Hammer's Frontend
6. Graph Validation and Shape Inference
7. Printing and Visualizing CUDA Hammer's IR

## Part 2 -- Basic Optimization Passes
8. The Pass Manager: Structuring Compiler Transformations
9. Constant Folding and Dead Code Elimination
10. Common Subexpression Elimination for Tensor Graphs
11. Algebraic Simplification

## Part 3 -- Operator Fusion
12. Why Fusion Matters: Memory-Bound vs. Compute-Bound Kernels
13. Elementwise Fusion
14. Reduction Fusion
15. Loop Fusion and Tiling
16. Fusion Boundaries: What Can't Be Fused, and Why

## Part 4 -- Code Generation
17. Lowering CUDA Hammer's IR to Loops
18. Generating CUDA C++ From the Fused IR
19. Generating Vectorized CPU Code
20. A JIT Backend: Compiling and Loading Generated Code at Runtime

## Part 5 -- Autotuning
21. The Search Space: Tile Sizes, Loop Orders, and Unrolling
22. Cost Models vs. Measurement-Based Autotuning
23. Building CUDA Hammer's Autotuner
24. Caching and Reusing Tuned Schedules

## Part 6 -- Case Studies: How CUDA Hammer Compares to Real Compilers
25. XLA's HLO and Fusion Passes
26. TVM's Relay/TIR and Ansor Auto-Scheduling
27. Triton's Block-Level Programming Model
28. torch.compile and TorchInductor
29. Flash Attention as a Fusion Case Study
30. Quantization-Aware Codegen

## Part 7 -- CUDA Hammer in Practice: Real-World Domain Applications (PLANNED, not yet chaptered)
A closing, capstone part: CUDA Hammer, exactly as built through Parts 1-6 (no new toolchain, no hidden extra machinery), pointed at real problems from different industries -- the deliberate "aura of sophistication" the earlier case-study chapters (Part 6, which studies OTHER real compilers) don't by themselves provide, since Part 6 is about how XLA/TVM/Triton/etc. work, not about CUDA Hammer itself doing real work. Candidate domains to choose chapters from when this Part is actually planned in detail (none committed yet): quantitative finance / risk models, computer vision preprocessing pipelines, NLP/transformer inference graphs, scientific computing and simulation kernels, recommendation-system scoring, robotics/control and edge inference. Exact chapter count and which domains make the cut: TBD -- revisit once Part 6 is underway so this Part's own numbering (currently a placeholder "Part 7") doesn't force a later renumber.

## Appendices
- A. Installation and Setup -- Building CUDA Hammer's Toolchain
- B. Practice Quiz
- C. LLVM and MLIR: The Compiler Infrastructure You Get for Free
- D. Polyhedral Compilation and Advanced Loop Transformations
- E. Profiling and Benchmarking Generated Kernels
- F. From torch.compile and TVM's Python API to C++: A Rosetta Stone
- G. Common Failure Modes: Miscompilation, Fusion-Order Drift, and Autotuner Overfitting
- H. Compiler Data Structures and Algorithms Primer (PLANNED, not yet written) -- background material collecting the general-purpose data structures and algorithms this book leans on throughout, in one reference place, separate from Part 1's own just-in-time introductions (Chapter 4's Value/Node/Graph, Chapter 4's Kahn's-algorithm topologicalSort(), Chapter 6's shape/broadcast tables): the DAG-as-node-list representation, topological sorting (both Kahn's algorithm and the DFS-based alternative, contrasted), symbol tables, worklist/queue-driven pass patterns, and hash-consing (relevant to Chapter 10's CSE). Framed as a prerequisite refresher a reader can consult before or alongside Part 1, not as new CUDA Hammer content -- general CS background, cited to general algorithms references (e.g. CLRS) rather than measured/executed the way the book's own chapters are.

## Status
Scaffolded 2026-09-21: mkdocs.yml, docs/index.md, docs/getting-started.md, this file.
Toolchain confirmed this session: cloud sandbox is x86_64, g++ 13.3.0, clang++ 18.1.3, real apt-installed nvcc 12.0 (genuine `nvcc`/`ptxas`/`cicc`, not the incomplete pip wheel the sibling books already documented), AVX2/AVX512F/FMA present in `/proc/cpuinfo`. Device (connected folder, isolated Linux VM on the user's Mac) is aarch64, g++ 11.4.0, no clang++, no nvcc -- but genuine NEON (`asimd`) confirmed by actually compiling and running an `arm_neon.h` test program there (output `3.0 3.0 3.0 3.0`, correct). This means Part 4's CPU-codegen chapter (Ch19) can genuinely compile AND RUN real vectorized kernels on two real, different architectures (AVX2/FMA on the cloud sandbox, NEON on the device) with zero simulation needed for that backend -- unlike the CUDA backend (Ch18), which compiles for a real arch via nvcc but cannot execute anywhere in this toolchain (no physical GPU on either side), the same honest limitation the sibling CUDA books already carry.
Chapter 1 (Part 0, DONE, commit `b1f1933`): "The Cost of Eager Execution" -- opens the book. Two real, independent costs of eager execution quantified without any fabricated timing: 1.1/1.3 a genuinely counted memory-traffic model (eager(N) = N * 2 * array_bytes vs. fused = 2 * array_bytes, checked for N=1..8, EAGER and FUSED outputs verified bit-identical); 1.2 NVIDIA's own real cited kernel-launch-overhead figure (9.6us per kernel, "Getting Started with CUDA Graphs" blog post) applied via a real formula to the same chain lengths, plus a closing synthesis on a concrete 4-op/1M-element example that deliberately reports the two reductions SEPARATELY rather than fabricating a combined wall-clock speedup. 2 files (both plain C++, no CUDA/NCCL/MPI linkage -- cross-verified on both cloud sandbox and device), 4 ASCII diagrams (chapter-opening + one per section), zero diagram-rule violations on first check.
[stated] Planning note (2026-09-22, not yet placed in the chapter/appendix plan): user asked to integrate NVIDIA CUTLASS (the real production GEMM/tensor-op template library) at some point. Natural homes to decide between when we get there: a new Part 6 case-study chapter ("How CUTLASS Generates Production GEMM Code"), or folding it into Appendix C alongside LLVM/MLIR as a second "infrastructure you get for free" example. Not yet assigned a chapter number -- revisit before Part 4 (codegen) or Part 6 (case studies) so numbering doesn't need to be redone later.
[stated] Planning note (2026-09-23, not yet chaptered): user asked to add a closing Part 7 using CUDA Hammer to build real applications across industry domains, explicitly to give the finished book "an aura of sophistication" -- logged above as "Part 7 -- CUDA Hammer in Practice: Real-World Domain Applications", candidate domains listed there, none committed. Revisit once Part 6 is underway.
[stated] Planning note (2026-09-23, not yet written): user asked for a new appendix collecting the common data structures used in building a compiler (graph representation, topological sorting, etc.) as background/reference material -- logged above as "Appendix H: Compiler Data Structures and Algorithms Primer". Meant as a prerequisite refresher, not new CUDA Hammer content.
[stated] Planning note (2026-09-23, not yet chaptered): more fusion types belong in the book eventually -- matmul/epilogue fusion (bias+activation fused onto a GEMM tile, ties to the existing CUTLASS planning note above), softmax/attention-style fusion (an online/streaming reduction, unlike Chapter 14's full-tensor-to-scalar Sum), and scan/prefix-sum fusion (blocked by a genuine sequential dependency Chapter 13's per-element-independence assumption doesn't allow). User's own explicit decision on placement: since Part 3 (Chapters 12-16) and Part 4 (Chapters 17-20) are both already built and committed, do NOT renumber either -- the natural home is either new chapters appended later as a "Part 3.5" (a second operator-fusion part, positioned after Part 3 closes but without renumbering Part 4 onward) or folded into Part 6's own case-study chapters (where XLA/TVM/Triton's real epilogue-fusion and attention-fusion approaches would come up naturally alongside this book's own attempt). Exact chapter numbers/placement not committed yet -- revisit once Part 6 is underway, same as the Part 7 and CUTLASS notes above.
[stated] Theme palette changed 2026-09-22: yellow/amber (was deep purple/purple).
Chapter 2 (Part 0, DONE, commit `c39c0bd`): "What a Compiler Pipeline Looks Like" -- completes Part 0's own build-up before Part 1 starts CUDA Hammer itself. Builds general compiler vocabulary (lex, parse, AST, lower, linear IR, pass, codegen) on a genuinely implemented tiny arithmetic-expression compiler, not tensors yet. 2.1/2.2 (File 003): real lexer + recursive-descent parser + AST + AST-to-three-address-code lowering + IR interpreter, cross-checked against an independent direct-AST evaluator across 5 expressions (precedence and left-associativity both genuinely exercised, e.g. `a - b - c`). 2.3 (File 004, reuses File 003's front end unchanged): a real constant-folding + constant-propagation pass operating purely on the IR, genuinely eliminating up to 100% of an expression's instructions (one test folds to zero instructions), correctness checked by interpreting before and after folding on every expression (all matched), 66.7% total instruction reduction across the 5 test expressions. 2 files, both plain C++ (no CUDA linkage), zero diagram-rule violations across 4 diagrams on first check. No external sources cited -- general compiler-construction vocabulary from first principles.
Chapter 3 (Part 0, DONE, commit `935b6a6`): "A Tour of Real ML Compilers: XLA, TVM, and Triton" -- closes Part 0. A citation-heavy survey chapter, not a code chapter (its own `code/` directory is intentionally empty, per Getting Started's own honesty discipline: claims about other projects are cited to their own real documentation, not measured by this book). Maps Chapter 2's vocabulary (IR, pass, lowering, codegen) onto three real production ML compilers: 3.1 XLA's HLO IR and its two-phase (target-independent then backend-specific) fusion passes, cited to openxla.org's own architecture/hlo_passes/gpu_architecture pages, including the direct quote "Fusion is XLA's single most important optimization"; 3.2 TVM's TensorIR and its explicit schedule primitives (`split`/`tile`/`reorder`/`bind`/`fuse`), cited to tvm.apache.org's own TensorIR and schedule-primitives docs, plus the DLight (rule-based) vs. MetaSchedule (search-based) distinction that foreshadows CUDA Hammer's own Part 5; 3.3 Triton's block-level "Blocked Program, Scalar Threads" model (inverted from CUDA's own "Scalar Program, Blocked Threads"), cited to triton-lang.org's own programming-guide introduction. 6 real sources cited, all fetched and quoted directly from each project's own current documentation this session. 4 ASCII diagrams, one stray `<-` caught and fixed by the automated diagram check before locking (replaced with `--`), zero violations after the fix.
[stated] Header bar text/icons changed to red 2026-09-23 (commit `9989464`), via `docs/stylesheets/extra.css` (`#c1121f`, wired in through `mkdocs.yml`'s `extra_css`) -- scoped to the header bar only, per the user's own explicit scope choice.
Chapter 4 (Part 1, DONE, commit `3170666`): "Representing Tensors and Operations as a Graph" -- opens Part 1, CUDA Hammer's first real code chapter (no more parsing arithmetic text -- direct graph-construction API calls from here forward). 4.1/4.2 (File 005): real `Value`/`Node`/`Graph` classes; a genuinely shared 6-node diamond DAG built only through `addInput`/`addBinary`/`addUnary` (never parsed from text), with a computed non-leaf node (`t1`) proven to have exactly 2 real consumers, plus a deliberately unmemoized recursive `expandToTreeNodeCount()` genuinely computing that an equivalent unshared tree would need 10 nodes instead of 6 -- a real counted IR-size cost of not sharing, echoing Chapter 1's own memory-traffic argument. 4.3 (File 006, reuses File 005's classes unchanged): a real topological sort (Kahn's algorithm), cross-checked by an independent positional validator, confirmed valid on the normal DAG; the same checker correctly detects a cycle in a second graph deliberately hand-broken by reaching past the public API (`mutableNode()`) to mutate an existing node's inputs after construction -- proving the normal API's cycle-freedom-by-construction guarantee by breaking it on purpose. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 6 ASCII diagrams total (4 hand-drawn + 2 locked verbatim program-output blocks, the latter correctly exempt from the diagram-character rule since they contain real printf output using `<-`), zero violations on first check. No external sources cited -- CUDA Hammer's own graph design is original to this book.
[stated] Starting with Chapter 5, chapters get MORE prose explanation and MORE ASCII diagrams before each code block, per the user's own explicit request mid-Chapter-5 -- same depth-first-then-code discipline already standing for the CUDA C++ DSA book, now applied here too.
Chapter 5 (Part 1, DONE, commit `12ac633`): "Building the Graph: CUDA Hammer's Frontend" -- Part 1 continues. A minimal, flat text format ("name = op(arg, ...)", every right-hand side a call, no exceptions) parsed straight into a Graph with NO AST stage -- a real architecture choice (the same one LLVM's own real IR text format uses), not a shortcut, since this grammar is flat rather than recursive. 5.1/5.2 (File 007): a tokenizer (6 token kinds) and a one-pass `parseStatement()`/`parseProgram()` calling straight into Chapter 4's own `addInput`/`addConst`/`addUnary`/`addBinary`, resolving every identifier argument through a `SymbolTable` the instant it's read. Correctness proven the strong way: parsing the Chapter 4 diamond program produces a graph `graphsStructurallyEqual()` confirms is node-for-node, edge-for-edge IDENTICAL to the graph Chapter 4 built by hand. 5.3 (File 008, reuses File 007's parser unchanged): four deliberately invalid programs (undefined name, redefinition, arity mismatch, unknown operation), each isolated to its own specific check inside `parseStatement()` and confirmed to throw a `std::runtime_error` naming the real problem, plus a fifth valid control program confirming none of those checks misfire on correct input. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Per the user's own mid-chapter request, this chapter carries substantially more prose (a full walk of the symbol table's state statement-by-statement, a token-by-token trace of one line, a table mapping each of the 4 error cases to the exact check that catches it) and more diagrams (9 total, up from the prior chapters' usual 4-6) than Chapters 1-4 -- this depth level now applies to Chapter 6 onward too. No external sources cited.
Chapter 6 (Part 1, DONE, commit `d26cdca`): "Graph Validation and Shape Inference" -- closes Part 1. Shape lives as a separate, computed side table (`map<nodeId, Shape>`), never a field on `Node` -- a deliberate pass/IR separation, not an oversight. 6.1/6.2 (File 009): the real NumPy-style broadcasting rule (cited to numpy.org's own docs, quoted directly), tested standalone on 5 hand-verified cases; `inferShapes()` reuses Chapter 4's own `topologicalSort()` completely unchanged to walk a graph in a guaranteed-valid order, computing each node's shape from its already-known inputs (leaves from a caller-supplied declaration, Add/Mul via broadcasting, ReLU shape-preserving) -- tested end to end on the Chapter 4 diamond graph, correctly inferring [3,4] through two broadcasts and a shape-preserving ReLU. 6.3 (File 010, reuses File 009 unchanged): 3 deliberately invalid graphs (2 flavors of incompatible shapes, 1 missing declared leaf shape), each isolated to the exact check that catches it, plus a valid control case. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 9 ASCII diagrams (continuing the Chapter 5 depth level), one stray `<-` in the opening diagram caught and fixed before locking (replaced with `, inputs`), zero violations after. 1 real external source cited (NumPy's own broadcasting rule).
Chapter 7 (Part 1, DONE, commit `395ccda`): "Printing and Visualizing CUDA Hammer's IR" -- closes Part 1. Two genuinely different external views of the same Graph. 7.1/7.2 (File 011): `printGraphAsSource()` emits Chapter 5's own exact "name = op(args)" grammar (using each input's debugName, never a numeric id) and is checked the strong way -- parseProgram(printGraphAsSource(g)) round-trips to a graph graphsStructurallyEqual() confirms is IDENTICAL to the original, both for the Chapter 4 diamond graph and for a second graph exercising the one special case (Const, whose argument is a number, not a name). Shows node-creation order is already a valid print order (same cycle-freedom-by-construction argument as Chapter 4), needing no topologicalSort() call. 7.3 (File 012, reuses Ch4 Graph/topologicalSort() and Ch6 Shape/inferShapes() unchanged): `exportToDot()` emits real Graphviz DOT (grammar cited directly to graphviz.org's own docs, quoted), with each node's label showing its Chapter-6-inferred shape too. Validated two ways: code-level structural counts (node count, edge count, out's in-degree), AND -- the strong claim -- graphviz actually installed in the cloud sandbox this session (`apt-get install graphviz`) and the generated file handed to the real `dot` binary, confirmed accepted (exit 0) by Graphviz's own parser, not just asserted to look like valid DOT. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device (Graphviz-binary validation step in File 012 gracefully skips if `dot` isn't installed, e.g. on the device, without failing the build). 9 ASCII diagrams (continuing the Ch5/Ch6 depth level); 3 diagrams initially flagged by the automated checker (a "<--"-annotated round-trip loop, several "<-" substitution annotations, a "<" less-than in prose, a "v" used as a downward arrow) and rewritten before locking -- zero violations after. 1 real external source cited (Graphviz's own DOT language grammar).
Chapter 8 (Part 2 opens, DONE, commit 7ef4fe6): "The Pass Manager: Structuring Compiler Transformations" -- Part 2 begins. Formalizes the analysis-pass/transform-pass distinction using Ch6's inferShapes() and Ch7's printGraphAsSource() as the analysis-pass examples already in hand. 8.1/8.2 (File 013): TransformPass = std::function<Graph(const Graph&)> -- a real design consequence of Graph having no node-removal API (motivated explicitly for Ch9's upcoming DCE); canonicalizeNodeNames(), the book's first real transform pass, renumbers every node's debugName to "%<id>" in topological order, checked the strong way -- graphsStructurallyEqual() (which never looks at debugName) confirms the computation is unchanged while names demonstrably changed, plus a check that the ORIGINAL graph's own names are untouched (proving the pass is read-only on its input, as the TransformPass contract requires). 8.3 (File 014, reuses File 013's types/passes unchanged): a real PassManager (`runPasses()`) threading a graph through a sequence of named passes, printing a before/after dump of EVERY pass via Ch7's own printGraphAsSource() (the first real use of that printer outside its own chapter) and checking DAG validity via Ch4's topologicalSort() after every single pass, not just at the end. Tested on a well-behaved 2-pass pipeline ([identityPass, canonicalizeNodeNames]) AND, the book's own every-chapter negative case, a deliberately broken pass (breakGraphByInsertingCycle(), reaching past the API via mutableNode() the same way Ch4's File 006 did) that runPasses() catches immediately, naming the exact pass responsible. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 9 ASCII diagrams (continuing the Ch5/6/7 depth level); 2 diagrams initially flagged by the automated checker (a "<" inside "map<int, Shape>" and inside a std::function<...> type signature, both rewritten in prose/words rather than literal angle-bracket syntax) and fixed before locking -- zero violations after (the one remaining "<" the checker flags is inside a locked verbatim Output block, exempt). No external sources cited -- pass/PassManager design is original to this book, built from vocabulary Chapters 2/4/5/6/7 already established; the general analysis-vs-transform-pass distinction is treated as general architectural knowledge (LLVM's own pass infrastructure is one well-known example), consistent with how Chapter 5 treated its own LLVM comparison.
Chapter 9 (DONE, commit 08f2414): "Constant Folding and Dead Code Elimination" -- the first genuinely optimizing passes, plugging directly into Chapter 8's own TransformPass/PassManager infrastructure with zero changes to that infrastructure. 9.1 (File 015): constantFoldPass() walks topologicalSort() order and folds Add/Mul/ReLU nodes into Const nodes by checking the NEW graph being rebuilt (not the old one), which lets an entire constant chain -- c1+c2, then that result *c3 -- fold in a single pass rather than needing to run to a fixed point; a new evaluate() interpreter is introduced alongside it, keyed by node NAME rather than id after a real bug (a genuine mid-session crash: the folded graph's topological order shifted `x` to a different id than it had originally, because c1/c2/c3/x are all simultaneously-ready leaves under Kahn's algorithm -- turned into the chapter's own [COMMON TRAP] admonition using the actual printed before/after output as proof, rather than silently fixed and hidden). 9.2 (File 016): deadCodeEliminationPass() -- the first real payoff of Chapter 8's "rebuild rather than mutate" design, stated as the reason that design exists. Introduces the explicit, honestly-stated convention that the LAST node in a graph is its own designated output (needed because Graph itself has no notion of "output" at all), computes backward liveness from that root via computeLiveNodeIds() walking node->inputs against topologicalSort()'s own forward direction, and rebuilds keeping only live nodes. Tested on a dead node built from real live inputs (not an obvious orphan), a node with two live consumers (kept exactly once, both consumers correctly remapped), and a control case (an already-clean graph comes back unchanged). 9.3 (File 017, reuses Files 015+016 verbatim plus Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass): runs [constantFoldPass, deadCodeEliminationPass] back to back through Chapter 8's own unmodified PassManager on a 7-node graph -- folding alone leaves node count unchanged (7->7, it only replaces computations, never removes nodes), DCE afterward drops it to 3 nodes, and a real code check confirms DCE run ALONE on the unfolded original graph removes nothing (7->7) -- substantiating "order matters" with an actual measurement rather than an assertion. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5/6/7/8 depth standard; zero ASCII-diagram-checker violations. No external sources cited -- constant folding and DCE are original design here, explicitly tied back to Chapter 2's own File 004 constant-folding pass (a different, simpler toy IR) as internal precedent rather than an external citation.
Chapter 10 (DONE, commit 29ebf41): "Common Subexpression Elimination for Tensor Graphs" -- Part 2's third optimizing pass, again plugging into Chapter 8's unmodified TransformPass/PassManager infrastructure with zero changes. 10.1 (File 018): commonSubexpressionEliminationPass() computes a canonical cseKey() for each node (an Input's own name; a Const's literal value; an Add/Mul/ReLU's op plus the NEW, already-rebuilt ids of its operands) and, walking topologicalSort() order, skips re-adding any node whose key already exists, remapping its consumers to the earlier node instead -- the same "just don't add it" idiom Ch9's DCE used, here because something IDENTICAL already exists rather than because nothing needs it. Because the key reads the NEW graph (oldIdToNewValue), a merge discovered early in the walk cascades into further merges discovered later in the SAME pass (demonstrated twice: a duplicate Add node test, and a duplicate-Const test where two independently-created Const(7) nodes merge and that merge cascades into merging the two Add nodes that consumed them, 6->4 nodes). Also includes a control case (no duplicates -> unchanged). 10.2 (File 019): characterizes cseKey()'s real boundary with actual executed evidence on BOTH sides -- add(a,b) and add(b,a) are mathematically identical but get DIFFERENT keys (operand order is encoded, not commutativity-aware) and are deliberately NOT merged, a real, honestly-stated conservative limitation (staying conservative avoids the much worse alternative: merging operands for an operation that ISN'T actually commutative, a silent correctness bug); contrasted against two separately-created Input nodes sharing a name, which DO correctly merge (cascading into their own consumers too), since Ch9's own evaluate() looks an input up purely by name, making that merge genuinely safe rather than a missed shortcut. 10.3 (File 020, reuses Ch9's constantFoldPass()+deadCodeEliminationPass()+evaluate() and Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass verbatim): builds a graph where an Add node ("p" = c1+c2) will fold to the exact same VALUE as an independently-created Const ("c3" = 5), but CSE alone cannot see this before folding runs (an Add node and a Const node have different cseKey() prefixes regardless of eventual value) -- proven by actually running CSE ALONE on the unfolded original graph first and confirming it finds nothing (8->8), the same "prove it, don't just assert it" discipline Ch9's 9.3 used for its own order-matters claim. The full [constantFoldPass, commonSubexpressionEliminationPass, deadCodeEliminationPass] pipeline then reduces the graph 8->8(fold)->6(CSE)->4(DCE) nodes, with the CSE merge itself creating NEW dead code (c1, c2, no longer referenced once "p" is elided in favor of "c3") that only the final DCE pass cleans up -- a fresh, three-pass-spanning instance of the exact "could running this again find more" question Ch9's own [COMMON TRAP] left open, extended from a new angle (folding creating a CSE opportunity, not just DCE cleanup) rather than resolved. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5-9 depth standard; zero ASCII-diagram-checker violations. No external sources cited -- CSE is standard, decades-old compiler-construction vocabulary, not specific to any one real compiler needing citation, same treatment Ch8 gave the general analysis-vs-transform-pass distinction.
Chapter 11 (DONE, commit 34406ca): "Algebraic Simplification" -- Part 2's fourth and closing optimizing pass, again plugging into Chapter 8's unmodified TransformPass/PassManager infrastructure with zero changes. 11.1 (File 021): algebraicSimplificationPass() is structurally the twin of Ch9's constantFoldPass() -- it walks topologicalSort() order and rebuilds via Ch4's addInput/addConst/addUnary/addBinary, but at an Add/Mul node it checks the NEW, already-rebuilt operands against arithmetic identities (additive identity x+0=x, 0+x=x) and, on a match, points the new Value directly at the ALREADY-EXISTING operand -- no new node built at all, not even a Const, a genuinely different "shape" of match than either constantFoldPass()'s new-Const-answer or CSE's reuse-an-earlier-duplicate. Demonstrated on a graph where add(x,0) and add(0,x) both elide to plain x while a control add(x,y) survives untouched, confirmed both structurally (elided names gone, survivor's both operands literally the same node) and by evaluate() agreement before/after. 11.2 (File 022, reuses File 021's pass verbatim): extends the identity table with the multiplicative identity (x*1=x, 1*x=x), the multiplicative absorbing element (x*0=0, 0*x=0 -- which DISCARDS the other operand entirely, a genuinely different shape of identity than the other three, since it throws away not just the redundant identity operand but the whole other side, along with anything IT depended on), and ReLU idempotence (relu(relu(x))=relu(x), checked by asking whether a ReLU's own already-rebuilt operand is itself a ReLU node). A deliberately-built 16-node test graph demonstrates a 3-level cascade discovered in a single pass -- two independent mul(_, 0)s both elide to the SAME zero constant, an Add combining those two now-identical zeros elides AGAIN to that same zero, and an Add combining that zero with something else elides a THIRD time to that something else -- 16->9 nodes, all elided names gone, every survivor correctly rewired, evaluate() agreement preserved. 11.3 (File 023, reuses Ch9's constantFoldPass()+deadCodeEliminationPass(), Ch10's commonSubexpressionEliminationPass(), and this chapter's own algebraicSimplificationPass() verbatim, plus Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass): closes Part 2 with all four of its passes running together through Chapter 8's still-completely-unmodified PassManager. Part A proves algebraicSimplificationPass() ALONE, run first, finds nothing on a graph whose simplification opportunity only exists after folding (13->13, the same "prove it, don't assert it" discipline every prior chapter in Part 2 has used for its own order-matters claim). Part B runs the full [constantFoldPass, commonSubexpressionEliminationPass, algebraicSimplificationPass, deadCodeEliminationPass] pipeline end to end: 13(original)->13(fold)->12(CSE merges a literal duplicate Add)->10(simplification elides two more nodes into aliases of earlier ones)->4(DCE sweeps up everything simplification's aliasing left newly dead), collapsing to the algebraic minimum add(x1,x2) squared, evaluate() agreement confirmed at 49 before and after. Part C is a small, deliberate, separate demonstration of a real hazard this chapter's own pass can trigger: when algebraicSimplificationPass() elides the LITERAL last node a graph ever added (here, a 3-node out=add(x,zero) where the whole graph elides to just x), Chapter 9's own "the last node is the graph's own designated output" convention -- which deadCodeEliminationPass() depends on directly -- silently ends up pointing at the WRONG node afterward (here, 'zero', not the true output), proven with real, minimal, executable code rather than asserted; framed explicitly as a concrete instance of a limitation Chapter 9 had already stated in the abstract (Worked Solution #4), with no fix offered -- an open question carried forward, the same honesty discipline every "could running this again find more" moment in Part 2 has followed. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5-10 depth standard; zero ASCII-diagram-checker violations after one stray "<-" (caught by the automated checker) was fixed. No external sources cited -- algebraic identities are general mathematical facts, not specific to any one real compiler needing citation.

Part 2 (Chapters 8-11) is now COMPLETE: The Pass Manager, Constant Folding and Dead Code Elimination, Common Subexpression Elimination, and Algebraic Simplification -- four TransformPasses, all built on Chapter 8's own unmodified PassManager/TransformPass infrastructure with zero changes made to that infrastructure across the whole of Part 2.

Chapter 12 (DONE, commit bceecc0): "Why Fusion Matters: Memory-Bound vs. Compute-Bound Kernels" -- opens Part 3, playing the same role for operator fusion that Chapter 1 played for the whole book: establishing, with real counted and cited evidence, exactly what the next several chapters (13-16) are trying to save. 12.1 (File 024): arithmetic intensity (totalFLOPs / totalBytesMoved) computed as a closed-form ratio for a chain of K unary elementwise ops -- AI_unfused = 1/(2*bytesPerElement), a CONSTANT independent of K (the K cancels completely, since traffic and work both scale by K in lockstep), while AI_fused = K/(2*bytesPerElement), GROWING linearly with K (fused traffic never grows past one read + one write, but fused work keeps growing) -- explicitly framed as the quantitative gap Chapter 1's own Worked Solution #7 already named as missing. 12.2 (File 025, reuses 024's two AI functions verbatim): the roofline model, real published work (Williams, Waterman, Patterson, CACM April 2009), with a real ridge point (peakFLOPs/peakBandwidth) computed from NVIDIA's own cited A100 datasheet (19.5 TFLOPS FP32, 1,555 GB/s) -- 12.5402 FLOPs/byte -- proving by direct computation that an UNFUSED chain is memory-bound at every tested K including 1,000,000 (its AI never moves), while a FUSED chain crosses into compute-bound at a real computed threshold, K=101, with the boundary (K=100 not compute-bound, K=101 is) confirmed exactly. 12.3 (File 026, reuses Ch4's Value/Node/Graph/topologicalSort() and Ch6's Shape/broadcastShapes()/inferShapes() completely unchanged, plus 025's ridge-point constants): moves from a hand-picked chain to a REAL Graph -- Chapter 4's own diamond (a,b inputs; t1=Add(a,b) with TWO consumers t2 and t3; out=Add(t2,t3)) -- counting unfused bytes (every non-leaf node's own kernel re-reads shared operands like 't1' once per consumer) vs. fused bytes (each distinct leaf read once via Ch9's "last node is the output" convention, output written once) directly from the graph's own Ch6-inferred shapes rather than a formula; found a real 4.4x AI improvement (0.096774 -> 0.428571 FLOPs/byte) even though both land in the same memory-bound classification at this toy graph's tiny scale (12 elements/node), honestly framed as evidence the ratio improvement is real at any scale while crossing the actual ridge point needs the far larger tensors Part 3's real fusion passes will operate on. 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-11 depth standard; one roofline ASCII diagram's stray "<-" annotations caught by the automated checker and fixed (replaced with "--"), zero violations after. 3 real external sources cited and fetched fresh this session: Williams/Waterman/Patterson's original 2009 CACM roofline paper (concept, not directly quoted -- full text is paywalled), NERSC's own roofline documentation (quoted directly for its arithmetic-intensity definition and "machine balance point" naming of the ridge point), and NVIDIA's own A100 datasheet PDF (fetched directly, FP32 TFLOPS and memory bandwidth figures verified before use). mkdocs build --strict clean; nav/TOC updated (new "Part 3 -- Operator Fusion" heading added); TOC's stale Chapter 11 "commit pending" placeholder also fixed in this same edit (self-correction pattern holding steady: Ch10->Ch11, Ch11->Ch12 -- keep checking at Ch13) -- push+build handoff given to user after this entry.

Chapter 13 (DONE, commit 8110e4d): "Elementwise Fusion" -- Part 3's first real fusion pass, and the first genuinely new IR node kind since Chapter 4: OpKind::FusedElementwise, whose body is a small ordered internal program (std::vector<FusedStep>, each step an Add/Mul/ReLU reading either an ExternalInput -- the node's own inputs list, i.e. memory, read once -- or a PriorStep -- an earlier step's result within the SAME node's program, i.e. a register, never memory). 13.1 (File 027): the new IR itself (Value/OpKind/OperandKind/FusedOperand/FusedStep/extended Node/extended Graph with addFusedElementwise(), a new ad hoc printGraph() debug printer since Ch7's printGraphAsSource() has no grammar for a node whose body is a program, and evaluate() extended to execute a fused node's internal steps into a local register array) plus elementwiseFusionPass() itself: walks topologicalSort() order: Input/Const rebuild trivially; single-consumer Add/Mul/ReLU are skipped (inlined later by their one consumer); everything else is materialized by resolveIntoGroup(), a recursive helper that inlines any single-consumer elementwise operand as a new FusedStep (recursing into ITS operands first) or treats it as an ExternalInput otherwise, deduplicating repeated references within one fused body via two id->index maps so a value read twice costs one memory read, not two; a 1-step result emits a plain node (no trivial wrapper), only >1 steps emit a real FusedElementwise node. Demonstrated on a straight single-consumer chain (mul->add->relu, all single-consumer): 6 nodes -> 4, one FusedElementwise node with 3 external inputs and 3 steps, evaluate() agreement confirmed at two different inputs. Honestly notes as a stated limitation (not fixed) that a value used TWICE by the SAME consumer (e.g. mul(t1,t1)) is outside this pass's own single-consumer-count rule as written. 13.2 (File 028, reuses File 027's entire type/pass machinery verbatim): proves a fusion boundary is forced EXACTLY when a node has more than one consumer, by hand-deriving the expected structure BEFORE running the code and then confirming an exact match, on Chapter 4's own diamond graph (t1=Add(a,b) has 2 consumers t2,t3 -- stays materialized as its own plain node -- while t2/t3/out, each single-consumer, fuse into one FusedElementwise node: external inputs [t1,a], steps [Mul,ReLU,Add], 6 nodes -> 4) versus a deliberately modified control graph with that one shared value removed (fuses completely into a single node, 4 nodes -> 3) -- same pass, unmodified, two different real outcomes because the GRAPH's own structure differs, not the pass. evaluate() agreement confirmed on both graphs. 13.3 (File 029, reuses 027/028's machinery plus Ch6's Shape/broadcastShapes()/inferShapes() and Ch8's TransformPass/NamedPass/runPasses() all unchanged, via a one-line elementwiseFusionTransform() wrapper since the pass's own richer FusionResult -- which also carries representativeOldId, a new-node-id->original-node-id map used as a side channel to reuse Ch6's shape inference across the pass without teaching it about the new node kind -- doesn't fit Chapter 8's plain TransformPass signature): the fifth real optimization pass (after constant folding, dead code elimination, CSE, and algebraic simplification) to plug into Chapter 8's PassManager unmodified, measured on Chapter 4's own diamond graph against BOTH of Chapter 12's own bounds -- bytes moved before fusion: 496 (exactly matching Chapter 12's own Section 12.3 figure), after fusion: 256, landing strictly between Chapter 12's idealized single-kernel bound (112, computed by a formula that never had to check real data sharing) and the fully-unfused figure (496) -- honestly framed as the real, achievable result once genuine sharing constraints (t1's 2 consumers) are respected, not a shortfall to explain away. AI improves 0.096774 -> 0.187500 FLOPs/byte (real, though still memory-bound at this toy graph's tiny scale, consistent with Chapter 12's own scale caveat). evaluate() agreement confirmed before/after fusion. Caught and fixed a real compile error during writing: an initial runPasses() draft used copy-assignment (Graph current = initial; ... current = next;), which fails because Graph owns vector<unique_ptr<Node>> and is deliberately non-copyable -- fixed by matching Chapter 8's own File 014 pattern exactly (first pass reads initial by const reference, every later pass moves its result into current via std::move). 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-12 depth standard; one diamond-fusion ASCII diagram's stray "<-" annotations and one "112 < 256 < 496" stray less-than comparison caught by the automated checker and fixed (replaced with "--"), zero violations after. No new external sources cited -- the FusedElementwise design is explicitly modeled on XLA's own real fusion computations, already cited in Chapter 3. TOC's stale Chapter 12 "commit pending" placeholder also fixed in this same edit (self-correction pattern holding steady: Ch11->Ch12, Ch12->Ch13 -- keep checking at Ch14) -- push+build handoff given to user after this entry.

Chapter 14 (DONE, commit 833f6dd): "Reduction Fusion" -- extends Chapter 13's own fusion machinery to OpKind::Sum, the first genuinely SHAPE-CHANGING operator this book's IR has needed. 14.1 (File 030): the new IR (OpKind::FusedReduction, structurally identical to FusedElementwise except its own LAST step is always Sum; FusedStep gains one new field, reduceElementCount, meaningful only for that step, baked in by the pass at fusion time from Chapter 6's inferShapes() extended with its own first new branch since Chapter 6 itself -- Sum maps to Shape{}, an empty-dims scalar) and reductionFusionPass(): resolveIntoGroup() (Ch13's own, reused) gains exactly ONE new condition -- a Sum node's own output is ALWAYS external, regardless of consumer count -- and the pass's own top-level walk gains a matching new rule: unlike Add/Mul/ReLU, a Sum node ALWAYS materializes as its own root even with exactly one consumer (Ch13's own "consumers==1, skip" shortcut would otherwise silently drop it from the output graph entirely, since nothing would ever be allowed to inline it). Demonstrated on x=Input,t1=ReLU(x),s=Sum(t1): t1 inlines into s's own FusedReduction body (2 steps: ReLU,Sum), 3 nodes -> 2, evaluate() agreement confirmed (needed extending evaluate() with an elementCounts parameter -- the first time this book's scalar-representative evaluate() has needed shape information at all -- and fixing a latent hazard in Ch13's own step-dispatch, which used a bare else to mean Mul, harmless there but a silent out-of-bounds read waiting to happen the moment a Sum step could exist). Also proves, by exact textual comparison via a new string-returning formatGraph(), that reductionFusionPass() run on one of Chapter 13's own Sum-free test graphs produces output IDENTICAL to elementwiseFusionPass()'s -- a strict generalization, not a separate pass with separate rules. 14.2 (File 031, reuses File 030's machinery verbatim): two graphs proving what a reduction's output forces downstream. Graph A isolates the new rule alone (s=Sum(relu(x)) has exactly ONE consumer y=Add(s,a), yet still cannot be inlined -- purely because s changed shape; 5 nodes -> 4). Graph B combines Ch13's sharing rule and Ch14's reduction rule in the SAME graph at once (t1 has 2 consumers -- stays external via the OLD rule; s has 1 consumer but is a reduction -- stays external via the NEW rule): 6 nodes -> 6, correctly fusing NOTHING at all, framed explicitly as the honest correct answer for a graph whose structure offers no safe fusion, not a failure. evaluate() agreement confirmed on both graphs (3 checks). 14.3 (File 032, reuses 030/031's machinery plus Ch8's TransformPass/NamedPass/runPasses() unchanged): plugs reductionFusionPass() into Ch8's PassManager via a CLOSURE (reductionFusionTransform captures a specific graph's own precomputed elementCounts, since TransformPass's own Graph(const Graph&) contract has no room for the second argument reductionFusionPass() needs -- a real new external dependency Ch13's own wrapper never had to carry) -- measured on a clean, unshared chain (a=[3,4],b=[4],t1=Add,t2=ReLU,s=Sum): 260 bytes before fusion, 68 after, a 73.8% reduction, the maximum this graph's own shape allows since nothing in it is shared. Also catches and fixes a real silent gap in Chapter 12's own totalFlops() convention ("one FLOP per output element"), which assumed output element count equals operation count -- true for Add/Mul/ReLU, false for Sum (N elements -> N-1 additions, not 1) -- corrected with one new branch; AI improves 0.134615 -> 0.514706 FLOPs/byte, both still memory-bound at this toy scale. evaluate() agreement confirmed. 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-13 depth standard (4 hand-drawn diagrams: opening + one per section); one stray "<<" caught by the automated checker and fixed (replaced with "vs"), zero violations after. No new external sources cited -- OpKind::Sum/FusedReduction extend Chapter 13's own FusedElementwise design in the same spirit already cited to XLA in Chapter 3. TOC's stale Chapter 13 "commit pending" placeholder also fixed in this same edit (self-correction pattern holding steady: Ch12->Ch13, Ch13->Ch14 -- keep checking at Ch15).

Chapter 15 (DONE, commit 36b1771): "Loop Fusion and Tiling" -- closes out the loop-level reasoning Chapters 13-14 never needed, built as a new VIEW on top of the existing IR (no new fields on Node or FusedStep). 15.1 (File 033): LoopNest (an ordered list of Loop{dimName, extent}) and buildLoopNest(), which reads ONLY information the IR already has -- a node's own Chapter-6-inferred output Shape (one loop per dimension, for Add/Mul/ReLU/FusedElementwise) plus, for a FusedReduction, one extra loop whose extent comes straight from Chapter 14's own already-baked-in reduceElementCount. Demonstrated on Chapter 13's own diamond graph (both t1 and the fused node share loop nest [dim0:3, dim1:4], 12 iterations, agreeing with Chapter 6's numElements()) and Chapter 14's own reduction graph (s's own loop nest is [reduce:4], agreeing with the Sum step's own baked-in count); a third test proves the loop nest's own total iteration count is INVARIANT across the fusion boundary -- fusion changes how s is computed, never how many iterations its reduction needs. 15.2 (File 034): TiledLoop/tileLoop()/actualInnerExtent(), splitting one loop of extent N into an outer/inner pair given a tile size, with the one real subtlety -- a boundary tile that doesn't divide evenly -- proven correct (not asserted) by literally enumerating the set of indices visited by the tiled vs. untiled forms and comparing them directly, across three cases (evenly dividing: 12/4; a genuine partial boundary tile: 13/4, last tile 1 element not 4; tile larger than the whole loop: 4/8) plus one real 2-D LoopNest from 15.1 tiled along one dimension (a 2-loop nest becomes a 3-loop nest, same 12 total iterations, same set of (dim0,dim1) points, proven by direct point-set comparison). 15.3 (File 035): loopNestsCompatibleForFusion(), a conservative, exact, ORDER-sensitive check (same loop count, same extents in order) answering a genuinely DIFFERENT question from Chapters 13-14's own operator-fusion rules -- demonstrated on Chapter 14's own Graph B (t1 stays plain because it's SHARED, s stays external because it's a REDUCTION, nothing fuses structurally, exactly as Chapter 14 found) where t1's own loop nest ([dim0:4]) and s's own loop nest ([reduce:4]) turn out COMPATIBLE anyway -- two nodes operator fusion correctly refused to merge could still, at codegen time, share one physical loop, the same separation Chapter 3's own TVM citation already established (`fuse` as its own schedule primitive, separate from whatever earlier pass grouped computations into stages). Also demonstrates two genuine incompatible cases (different loop counts; same count, different extent) and one honestly stated limitation (order-sensitivity misses a permuted-but-equal match, [3,4] vs [4,3], left open rather than silently patched, the same conservative instinct as Chapter 10's own cseKey()). 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-14 depth standard (4 hand-drawn diagrams: opening + one per section); zero ASCII-diagram-checker violations on first check. No new external sources cited -- the loop-nest/tiling/loop-fusion separation is the same one already cited to TVM's own real schedule primitives in Chapter 3. (Chapter 14's own commit hash, 833f6dd, was already correct when this entry was added -- nothing stale to fix here. This chapter's own commit hash above, 36b1771, is now correct too -- Chapter 16's own edit has nothing to fix here; keep checking at Ch16 anyway, per the standing habit.)

Chapter 16 (DONE, commit 14817a0): "Fusion Boundaries: What Can't Be Fused, and Why" -- closes Part 3. 16.1 (File 036): classifyNode()/BoundaryReason, a diagnostic taxonomy (Leaf, GraphRoot, Shared, Reduction) that re-derives Chapters 13-14's own resolveIntoGroup() conditions as an explicit, inspectable reason list rather than a side effect of deciding whether to inline -- checked, not just asserted, via verifyClassificationAgreesWithPass() confirming its predictions against the real passes' own actual output on Chapter 13's diamond and Chapter 14's own Graph B (which genuinely has TWO independent zero-consumer GraphRoot sinks, y and y2, since it was never run through deadCodeEliminationPass() -- a real correction to the naive assumption that "the designated output" is always unique). 16.2 (File 037): a genuinely new KIND of boundary -- a cost-based size cap (maxChainLength) with nothing to do with correctness. Explains why a LIVE check inside resolveIntoGroup()'s own recursion cannot work (that recursion resolves a chain's deepest node FIRST, so steps.size() is always 0 at every call's own entry, no matter how long the chain is) and instead computes computeSizeCapBoundaries() as a separate pre-pass (chain-depth-based, cut every maxChainLength-th node), checked by resolveIntoGroup() as one more static condition -- plus the same top-level-walk companion fix Ch14 needed for Sum (the "consumers==1, skip" shortcut must also check the cap, or a capped node would vanish). Demonstrated on an isolated 4-node chain (maxChainLength=2): uncapped fuses all 4 steps into 1 node (36 bytes); capped splits into 2 FusedElementwise nodes of 2 steps each (72 bytes) -- a real, measured, HONEST cost of imposing the cap (72 > 36), while still far below the 140-byte unfused figure. evaluate() agreement confirmed across all three. 16.3 (File 038, capstone): boundedReductionFusionPass() combines Ch14's Sum condition and 16.2's size cap into one pass, plugged into Ch8's still-unmodified PassManager, run on one 10-node graph exercising EVERY boundary category at once (a 4-deep chain triggering the cap at depth 3, a shared value, a reduction, and the graph's own designated output) -- 508 bytes unfused -> 176 uncapped fusion (65.4% reduction, the maximum this graph's structure allows) -> 244 capped fusion maxChainLength=3 (52.0% reduction, strictly worse than uncapped, strictly better than unfused). evaluate() agreement confirmed across original/uncapped/capped (all = 330 at a=2,b=3). Closing note honestly states real fusion boundaries this book's own single-output, layout-free IR has never had to model (memory layout, control flow, multi-output kernels) rather than implying the taxonomy is exhaustive for tensor compilers in general. 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-15 depth standard (4 hand-drawn diagrams: opening + one per section); zero ASCII-diagram-checker violations on first check. No new external sources cited -- the size-cap boundary is motivated by the same real-compiler concern XLA/TVM/Triton all address (Chapter 3), without attributing a specific number or policy to any of them. TOC's stale Chapter 15 "commit pending" note above already carried its real hash (36b1771) when this entry was added -- nothing stale to fix here; this chapter's own commit hash above is what Chapter 17's edit will need to fix -- keep checking at Ch17.

Part 3 (Chapters 12-16) is now COMPLETE: Why Fusion Matters, Elementwise Fusion, Reduction Fusion, Loop Fusion and Tiling, and Fusion Boundaries -- five chapters building CUDA Hammer's own fusion machinery from a real measured motivation through two genuinely different IR extensions (FusedElementwise, FusedReduction), an independent loop-level view (LoopNest/tiling/loop-fusion-compatibility), and a closed, checked taxonomy of exactly when fusion has to stop -- correctness-driven and, for the first time, cost-driven too.

Chapter 17 (DONE, commit fa4582d): "Lowering CUDA Hammer's IR to Loops" -- opens Part 4, Code Generation, in a new docs/part4/ directory. 17.1 (File 039): evaluateArrays(), a real per-element interpreter (std::vector<float> per node) replacing the one-float-per-node simplification every evaluate() call since Chapter 9 has relied on -- checked for continuity against the OLD scalar evaluate() under UNIFORM inputs (Part A: all 12 elements of the diamond graph's own "out" node equal evaluate()'s own reported 15, for both the original and fused graphs), then against genuinely NON-uniform inputs no earlier chapter's own checks could express (Part B: 3 positions hand-derived BEFORE running -- out[0]=22, out[5]=182, out[11]=676 -- confirmed exactly, plus full per-element original-vs-fused agreement across all 12 positions, not just in aggregate), and on a reduction graph (Part C: relu then sum of a 6-element non-uniform array, hand-derived sum=11, confirmed on both the unfused and fused graph). 17.2 (File 040): lowerNode() (synthesizes a uniform LoweredNode -- FusedStep list + LoopNest -- for both already-fused and plain unfused nodes, so codegen never needs two code paths) and generateLoopFunction(), which emits REAL C++ text: nested for-loops from a node's own LoopNest (Chapter 15), a per-element or per-reduce-step program from its own FusedStep body, broadcast-safe external-input reads via the same "% extCounts[i]" rule Section 17.1 established. Compiled via a plain g++ shell-out (popen), run as a real separate process, and diffed against evaluateArrays() -- on t1=Add(a,b) (a plain node, exercising broadcast-safe indexing on a real compiled binary for the first time) and on the diamond's own fused "out" node (2 external inputs, 3 internal steps). Both confirmed exactly. Honestly notes the deferred JIT gap: Chapter 20 will replace this shell-out-and-read-stdout harness with real in-process dlopen loading; this chapter doesn't need to avoid a process launch to prove correctness. 17.3 (File 041, capstone): generateFullProgram() lowers an ENTIRE fused graph into one chained, compiled, executed program -- one generated function per non-leaf node, buffers threaded through a std::map<string,vector<float>>, every buffer printed and diffed against evaluateArrays() on the SAME fused graph. Run on Chapter 16's own 10-node capstone graph (boundedReductionFusionPass(), reused verbatim) TWICE: UNCAPPED (5 nodes: a,b,t5 FusedElementwise 5 steps,s FusedReduction,y FusedElementwise 2 steps) and CAPPED maxChainLength=3 (6 nodes: a,b,t3 FusedElementwise 3 steps,t5 FusedElementwise 2 steps,s FusedReduction,y FusedElementwise 2 steps) -- two DIFFERENT generated .cpp programs, DIFFERENTLY structured, both compiled and run as separate processes, both confirmed to match evaluateArrays() buffer-by-buffer AND to report the exact same final y=132.0 at a=[1,-2,3,-4,5,-6,7,-8],b=[2] -- making Chapter 16's own "different fusion structure, same math" claim real and executable for the first time, not just evaluate()-agreement on one representative scalar. 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-16 depth standard (4 hand-drawn diagrams: opening + one per section); one diagram-checker pass caught 2 stray "<" characters from quoted C++ syntax (std::vector<float>, "i0 < 3") inside hand-drawn diagrams and fixed both by rephrasing in prose -- the Output blocks' own literal "<" characters (real generated C++ printed by the programs themselves) correctly left untouched as locked verbatim output. No new external sources cited -- this chapter's own scalar-loop backend and shell-out harness are original to this book, building directly on Chapter 15's own LoopNest view. TOC's stale Chapter 16 "commit pending" note above already carried its real hash (14817a0) when this entry was added -- nothing stale to fix here; this chapter's own commit hash above is what Chapter 18's own edit will need to fix -- keep checking at Ch18.

Chapter 18 (DONE, commit 62e5734): "Generating CUDA C++ From the Fused IR" -- Part 4 continues. Opens with a real, throwaway toolchain investigation (3 test programs, written, run, deleted before this chapter's own real files started) establishing exactly what this chapter's own verification strategy could rely on: the cloud sandbox's own real nvcc 12.0 compiles a COMPLETE CUDA program (kernel + cudaMalloc/cudaMemcpy/launch/cudaFree) into a real ELF executable with no physical GPU present; running it WITHOUT checking CUDA error codes silently returns garbage (exit 0, no crash); running it WITH every call wrapped in a CUDA_CHECK macro honestly reports "no CUDA-capable device is detected" at every single call -- the verification strategy this whole chapter checks against, not a guess. 18.1 (File 042): emitSteps(), a codegen core shared UNCHANGED between a CPU wrapper and a CUDA wrapper -- called twice with the identical FusedStep list, isolating the only two genuine backend differences (how an external input gets read: an indexed buffer array on the CPU vs. a NAMED device pointer parameter on the GPU, since a std::vector's own buffer lives in host memory and would be an invalid device address dereferenced from a __global__ kernel; and how ReLU's own max gets spelled: std::max vs. fmaxf, since plain std::max isn't guaranteed to compile in __device__ code) -- and generateCudaElementwiseKernel(), one THREAD per output element instead of one more loop pass. Demonstrated on the diamond graph's own fused "out" node: same step count/sequencing confirmed from both Target::Cpu and Target::Cuda calls; a full generated .cu program (CUDA_CHECK-wrapped) compiles clean with nvcc, runs, honestly reports no device; a separately generated+compiled(g++)+EXECUTED CPU reference (same emitSteps() body) matches evaluateArrays() exactly (12 values, all confirmed). Hit and fixed one real compile error along the way: g++ needs an explicit "-x c++" flag to compile a .cu-extension file as plain C++ (without it: "file format not recognized; treating as linker script"). 18.2 (File 043): generateCudaReductionKernel() -- explains concretely why launching one thread per REDUCE element and doing "*out += step;" directly is a genuine data race (non-atomic read-modify-write on the same address, no ordering guarantee, classic lost-update bug), fixed with atomicAdd() (one hardware-guaranteed atomic add per thread) -- explicitly stated as the CORRECT approach, not the FASTEST one (a real high-performance reduction would use a shared-memory tree reduction or warp-shuffle instructions, one atomicAdd per BLOCK not per THREAD -- named as a scope limitation, not implemented). Demonstrated on Chapter 17's own reduction graph (relu then sum of [-3,5,-1,2,0,4], hand-derived sum=11): nvcc compiles the atomicAdd()-based kernel clean, running it honestly reports no device (cudaMemset-before-launch correctness detail included and explained), and the real executed CPU counterpart confirms 11.0, matching both the hand-derivation and evaluateArrays(). 18.3 (File 044, capstone): generateFullCudaProgram() -- one __global__ kernel per non-leaf node (dispatched by OpKind to the 18.1 or 18.2 generator), a real host main() (cudaMalloc every buffer, cudaMemcpy leaves in, sequential CUDA_CHECK'd kernel launches in topological order, cudaMemcpy the final "y" back) -- and generateFullCpuProgram(), the same node-by-node structure generated instead with the CPU-side emitSteps() functions and chained exactly like Chapter 17's own generateFullProgram(). Run on Chapter 16's own 10-node capstone graph (boundedReductionFusionPass(), reused verbatim) TWICE: UNCAPPED (5 nodes -> 3 CUDA kernels) and CAPPED maxChainLength=3 (6 nodes -> 4 CUDA kernels) -- both CUDA programs compile clean with nvcc and honestly report no device; both real, EXECUTED CPU counterparts match evaluateArrays() on every single buffer of both fused graphs AND report the identical final y=132.0 -- extending Chapter 16 and 17's own "different fusion structure, same math" claim to a real CUDA target for the first time. 3 files, all .cu / CUDA-linked -- verified ONLY in the cloud sandbox (the device has no nvcc at all), byte-identical delivery to the device confirmed by content hash, not re-compiled there. Diagram-heavy per the Ch5-17 depth standard (5 hand-drawn diagrams: opening + one per section, plus one inside a COMMON TRAP note); one diagram-checker pass caught 6 violations across 3 diagrams (a standalone "v" used as a down-arrow; several "<-" left-pointing annotations; one CUDA launch syntax fragment "<<<...>>>"; two left-pointing merge arrows "<---+"/"<-+") and fixed all of them by removing the banned characters -- restructuring the affected diagrams to use only plain "+"/"|"/"-" junctions (matching Chapter 17's own capped/uncapped graph diagram style) instead of arrowheads that would need "<" or a standalone "v" -- zero violations after. No new external sources cited -- this chapter's own shared emitSteps() core, CUDA kernel generators, and atomicAdd()-based reduction design are original to this book, verified against this book's own directly observed nvcc toolchain behavior rather than any external source. TOC's Chapter 17 entry above already carried its real hash (fa4582d) when this entry was added -- nothing stale to fix here.

Chapter 19 (DONE, commit e18bfb0): "Generating Vectorized CPU Code" -- Part 4 continues, and for the first time in Part 4, targets a backend this toolchain can genuinely compile AND EXECUTE: real SIMD intrinsics on two different real architectures this book actually has access to (AVX2/FMA on the cloud sandbox's x86-64, NEON on the device's aarch64). Opens with a real, throwaway toolchain investigation (2 test programs, written, run, deleted before this chapter's own real files started): `g++ -mavx2 -mfma -dM -E` confirms `__AVX2__`/`__FMA__`/`__x86_64__` on the cloud sandbox and a `_mm256_fmadd_ps(a,b,c)` test program computes `a*b+c` correctly (7.0=2*3+1); `g++ -dM -E` with NO special flags confirms `__ARM_NEON`/`__aarch64__` on the device (NEON is the mandatory aarch64 baseline, not an optional extension) and a `vfmaq_f32(c,a,b)` test program computes `c+a*b` correctly (7.0=1+2*3) -- the SAME three operands as AVX2's own fmadd, but with the accumulator argument FIRST instead of last, the load-bearing fact `vecFma()` exists to hide; also surfaces `vaddvq_f32` as a NEON-only single-instruction horizontal sum, noted as a scope-limitation aside, not implemented. 19.1 (File 045): `Isa{Avx2,Neon}` and five primitives (`vecLoadOrBroadcast()`/`vecAdd()`/`vecMul()`/`vecMax()`/`vecStore()`) hiding both architectures' own intrinsic names behind one generator; `canVectorizeElementwise()` (true only when every external input's own count equals the output count or is 1) gates a main vector loop plus a scalar tail reusing Chapter 18's own `emitSteps()` unchanged. Demonstrated on a=Input[11] (deliberately not divisible by 8 or 4, forcing a genuine tail on both ISAs), b=scalar, t1=Add,t2=ReLU,t3=Mul -- fused via plain `reductionFusionPass()` -- confirming `canVectorizeElementwise()` true here and FALSE for a genuine Chapter 6-style trailing broadcast (a:[3,4],b:[4]), the scope limitation caught, not silently miscompiled; compiled and RUN FOR REAL with the host's own real flags, matching `evaluateArrays()` exactly on both the cloud sandbox (n=11, width=8, 1 full iteration + 3 tail elements) and the device (n=11, width=4, 2 full iterations + 3 tail elements). 19.2 (File 046, reuses 045's machinery verbatim): `vecFma()` hides the AVX2-vs-NEON argument-order difference behind one call; a two-pass fold-detection generator (`computeStepUseCounts()`, `stepUsesOperand()`, `generateVectorElementwiseFunctionFma()`) recognizes a Mul step immediately followed by a consuming Add and folds them into ONE fused instruction instead of two -- proven safe not by a constructed counterexample but by tracing back to Chapter 13's own five-chapters-old single-consumer inlining invariant (`consumers.at(oldId) != 1` forces externalization, so within one fused group every non-final step has exactly 1 reader, by construction). Demonstrated on a positive case (Mul-then-Add, n=9, exactly 1 fold, folded FMA program compiled+run for real, matches `evaluateArrays()`) and a negative case (ReLU-then-Add, correctly 0 folds -- the pattern-match doesn't misfire) -- confirmed on both cloud (AVX2/FMA) and device (NEON, real `vfmaq_f32` accumulator-first argument order observed in the generated code). 19.3 (File 047, capstone): a genuinely new vectorized-REDUCTION technique (`generateVectorReductionFunction()`: a running vector accumulator, collapsed to one scalar by a portable array-extraction horizontal sum ONCE after the loop -- deliberately not NEON's own faster single-instruction `vaddvq_f32`, the same "correct, not fastest" honesty as Chapter 18's own atomicAdd() discussion) plus a full per-node dispatcher (`generateFunctionForNode()`: reduction nodes always take the new vectorized-reduction path; elementwise nodes take 19.2's FMA-fold path when `canVectorizeElementwise()` allows it, Chapter 18's own scalar path otherwise). Part 1 proves the scalar fallback is genuinely correct, not just "not vectorized": a real trailing-broadcast graph (a:[3,4],b:[4]) is correctly rejected by the scope check, the dispatcher chooses SCALAR-FALLBACK, and that generated scalar function is compiled and RUN for real, matching `evaluateArrays()`. Part 2 runs Chapter 16's own 10-node capstone graph (`boundedReductionFusionPass(maxChainLength=3)`, reused verbatim -- the SAME graph Section 17.3 and 18.3 both already used) through the dispatcher: all 4 materialized nodes (t3 FE-3st, t5 FE-2st, s FR-1st, y FE-2st) take a VECTOR path (0 scalar fallbacks -- nothing in this graph's own shapes falls outside scope), 3 total FMA folds fire (inside t3, t5, and y), and the whole generated program -- one real vector function per node, chained through named buffers exactly like Chapter 18's own CPU driver -- compiles and RUNS on both architectures, reporting y[0]=132.0, matching every single buffer against `evaluateArrays()` on both the cloud sandbox and the device. 3 files, all plain C++ (no CUDA linkage) -- unlike Chapter 18's own .cu files, cross-verified with a REAL compile+run on BOTH the cloud sandbox and the device (not just byte-identical delivery), the strongest hardware-verification story of any Part 4 chapter so far. Diagram-heavy per the Ch5-18 depth standard (3 hand-drawn diagrams: opening two-architecture comparison + one per section); one diagram-checker pass caught 6 violations (two `#include <...>` fragments with real angle brackets, a standalone `v` parameter name matching the down-arrow pattern, two `<placeholder>`-style angle-bracket comments in the vectorized-reduction diagram) and fixed all of them by rephrasing without angle brackets or renaming the parameter -- zero violations after. No new external sources cited -- this chapter's own `Isa` abstraction, vector primitives, FMA-fold peephole optimization, and vectorized-reduction technique are all original to this book, verified against this chapter's own directly observed AVX2/FMA and NEON toolchain behavior. TOC's Chapter 18 entry above already carried its real hash (62e5734) when this entry was added -- nothing stale to fix here.

Chapter 20 (DONE, commit ccbe790): "A JIT Backend: Compiling and Loading Generated Code at Runtime" -- closes Part 4. Opens with a real toolchain investigation (no throwaway test program needed this time -- the load-bearing fact came from a direct `uname -a` check already on record): the device, physically Apple Silicon hardware, is confirmed a genuine aarch64 LINUX virtual machine (`Linux claude 6.8.0-138-generic ... aarch64 GNU/Linux`), not native macOS; a throwaway `.so` compiled with `-shared -fPIC` and inspected with `file` reports a real `ELF 64-bit LSB shared object, ARM aarch64` -- the SAME object format the cloud sandbox's own x86-64 machine produces, meaning this chapter's own `dlopen()`/`dlsym()`/`dlclose()` mechanism, `-shared -fPIC`, and `-ldl` need ZERO per-architecture special-casing, a genuinely simpler mechanical story than Chapter 19's own real AVX2-vs-NEON split. 20.1 (File 048): `extern "C"` (disables C++ name mangling so a compiled symbol can be found by its plain, predictable name -- changes LINKAGE only, never the type system) applied to Chapter 18's own scalar elementwise generator; `JitModule`, an RAII owner of one `dlopen()` handle modeled directly on Chapter 4's own non-copyable, move-only `Graph`; `compileToSharedLibrary()`, the one remaining `popen()` shell-out, honestly scoped to COMPILATION only (a true self-hosting JIT would embed the compiler as a library -- LLVM's ORC JIT or libclang -- named as a real, out-of-scope limitation). Demonstrated on a 3-step fused node (Add, ReLU, Mul, 2 external inputs): `dlopen()`'d, `dlsym()`'d by its plain name `"compute_out"`, called directly with real C++ arguments, matching `evaluateArrays()` exactly -- no subprocess, no parsed stdout anywhere in that call. Closes on a real dangling-pointer risk: the `JitModule` used in the worked example goes out of scope right after its one call, `dlclose()`s, and the `ElementwiseFn` pointer this program still holds becomes genuinely dangling -- the fix stated as a lifetime rule, not a runtime check. 20.2 (File 049): `generateJitProgramForGraph()` -- walks a whole graph in topological order and packs EVERY non-leaf node's own `extern "C"` function into ONE compiled shared library, instead of one function (or one whole generated program) at a time; relies on Chapter 8's own five-chapters-old `canonicalizeNodeNames()` uniqueness guarantee to prove `"compute_" + debugName` can never collide within one graph -- and if it ever did, states the real, checkable "duplicate symbol" linker failure that would result, not a silent miscompile. Demonstrated on a 3-non-leaf-node graph (`t2` FusedElementwise-2st, `s` FusedReduction-1st, `y` FusedElementwise-2st) deliberately exercising BOTH generator kinds in one `.so`: exactly 1 `g++` invocation confirmed (via a real `compileInvocationCount` counter) for the whole graph, all 3 functions `dlsym()`'d from ONE `JitModule` and called directly, matching `evaluateArrays()` on every buffer. 20.3 (File 050, capstone): proves the loader is BACKEND AGNOSTIC -- Chapter 19's own vector-FMA-and-reduction generators wrapped in `extern "C"` for the first time (`generateExternCVectorElementwiseFunctionFma()`, `generateExternCVectorReductionFunction()`, `generateJitFunctionForNodeVectorized()`), plus a new `runJitGraph()` driver SHARED by both the scalar and vector backends (a `compute_t3` symbol looks identical to the loader regardless of which backend generated its body). Chapter 16's own 10-node capstone graph (`boundedReductionFusionPass(maxChainLength=3)`, reused verbatim -- the SAME graph Sections 17.3, 18.3, and 19.3 have all already used) is JIT-compiled TWICE, once per backend (vector backend needs `-mavx2 -mfma` on x86-64, nothing extra on aarch64 -- the one place the two backends' own compile commands genuinely diverge) -- both report `y[0]=132.0`, the vector backend confirming the same 3 FMA folds Section 19.3 already established for this graph. Closing tally, deliberately honest: `compileInvocationCount` reaches 2 (one per backend, roughly the same count Chapters 17-19's own harness already needed per backend -- no claim that compilation got cheaper); `executionSubprocessCount` stays exactly 0 across all 8 real results (4 nodes x 2 backends) -- the first time in this book EVERY result of a chapter's own capstone graph crossed from generated code into this program as a genuine function return, never a subprocess launch, never parsed stdout text. 3 files, all plain C++ (no CUDA linkage) -- cross-verified with a REAL compile+run on BOTH the cloud sandbox and the device (048/049 identical output both machines; 050 numerically identical on both, differing only in the expected AVX2/FMA-vs-NEON backend label). Diagram-heavy per the Ch5-19 depth standard (4 hand-drawn diagrams: opening harness-before/after + toolchain confirmation + one per section); one diagram-checker pass caught 2 standalone-`v` violations (down-arrow characters in the two-backend compile-flow diagram) and fixed both by replacing the vertical arrows with "+-->" branch junctions -- zero violations after. No new external sources cited -- this chapter's own `JitModule` design, `extern "C"` wrapping discipline, and whole-graph JIT compilation technique are all original to this book, building on Chapter 4's own non-copyable resource-ownership discipline and Chapter 8's own node-name uniqueness guarantee. TOC's Chapter 19 entry above already carried its real hash (e18bfb0) when this entry was added -- nothing stale to fix here.

Chapter 21 (DONE, commit c6d3ef6): "The Search Space: Tile Sizes, Loop Orders, and Unrolling" -- opens Part 5, Autotuning, and every backend through Part 4 (scalar, vectorized, JIT-loaded) compiled exactly ONE fixed schedule per fused kernel; this chapter builds and validates the SEARCH SPACE a later autotuner will need, deliberately stopping short of generating scheduled code (Chapter 22's own job, needing a cost model first) or running an actual search (Chapter 23's own job) -- every worked example still compiles and runs for real, computing genuine numbers about the search space itself. 21.1 (File 051): `enumerateTileSizeCandidates()` -- powers of two plus the loop's own full extent, the same heuristic real autotuning systems use (aligning with cache-line sizes, Chapter 19's own SIMD widths, and conventional GPU thread-block sizes), trading search COMPLETENESS for TRACTABILITY against a naive "every integer 1..extent" search. Extends Chapter 15's own set-equality proof (tiled index set == untiled index set) from one hand-picked tile size to the WHOLE candidate list, on two extents (13, not a power of two, forcing the "append the extent" branch; 16, exactly a power of two): 5 candidates each, all individually confirmed correct, against a naive 13-candidate search for the first loop alone. 21.2 (File 052): `LoopOrder`/`enumerateLoopOrders()` -- every permutation of a LoopNest's own loops via `std::next_permutation`; extends Chapter 15's own proof-by-enumeration discipline from tile boundaries to loop ORDER, proving (via `enumerateVisitedPoints()`, recording the full coordinate tuple under a given order) that BOTH possible orders of Chapter 15's own diamond-shaped loop nest (`dim0:3, dim1:4`) visit the exact same 12 index pairs -- for an elementwise nest, order is a pure performance knob, never correctness. Then a real, measured counterexample for reductions: float32's 24-bit mantissa means every integer up to 2^24=16,777,216 is exact, but past that the ULP exceeds 1.0, so summing 16777216.0 plus sixteen 1.0's in FORWARD order (big value first) silently absorbs all 16 increments (result unchanged at 16777216.0) while REVERSE order (small values summed first, exact at 16.0, then added once) recovers the true 16777232.0 -- a genuine 16.0 discrepancy between two equally valid summation orders, directly explaining why every `evaluateArrays()`-vs-generated-code check since Chapter 17 has used a TOLERANCE (1e-3f/1e-2f) instead of exact equality. 21.3 (File 053, capstone): `unrollFactorCandidates()` reuses 21.1's own candidate logic unchanged (tile size and unroll factor are the same underlying question -- how many elements per loop trip -- for two different reasons); `computeUnrolled()` demonstrates a real "main loop + scalar tail" unrolled computation (the software analogue of Chapter 19's own SIMD width) on extent=13 with factors 2/4/8, every result checked BIT-FOR-BIT identical to the un-unrolled baseline -- a direct contrast with 21.2's own reduction-order finding: unrolling and loop order are BOTH pure performance knobs for elementwise code, but ONLY loop order can change the answer for a reduction. Closes with `Schedule{tileSizePerLoop, loopOrder, unrollFactor}` and `enumerateSchedules()` (a plain odometer-style cartesian product, no library needed), run on a concrete `LoopNest [dim0:6, dim1:8]`: 4 tile candidates x 4 tile candidates x 2 loop orders x 4 unroll candidates = 128 real schedules, confirmed to match `enumerateSchedules()`'s own actual count exactly -- for a TINY 2-loop, 48-iteration kernel, motivating Chapter 22's own cost model (rank without measuring every candidate) and Chapter 23's own actual search. 3 files, plain C++ (no CUDA linkage) -- cross-verified byte-identical AND output-identical on both the cloud sandbox and the device (fully deterministic, no arch-specific labels this chapter, unlike Ch19/20). Diagram-heavy per the Ch5-20 depth standard (4 hand-drawn diagrams: opening 3-dimension overview + one per section); zero diagram-checker violations on the first pass. No new external sources cited -- this chapter's own tile-size/unroll-factor heuristics, loop-order enumeration, and cartesian-product schedule space are all original to this book, building on Chapter 15's own LoopNest/tileLoop() machinery and Chapter 19's own vector-width shape; the float32 non-associativity fact is a well-known IEEE 754 property, verified here by this chapter's own directly executed and measured example rather than an external citation. TOC's Chapter 20 entry above already carried its real hash (ccbe790) when this entry was added -- nothing stale to fix here.

Chapter 22 (DONE, commit d5f9656): "Cost Models vs. Measurement-Based Autotuning" -- picked up exactly where Chapter 21's own closing number left off (128 legal schedules for a toy two-loop kernel) and built two real cost models, each honestly tested against real hardware on both the cloud sandbox and the device. Deliberately scoped AWAY from a cache-reuse cost model: this book's own IR through Chapter 20 has no operator with genuine cross-element data reuse (every elementwise op and the full-reduction `Sum` reads each input exactly once regardless of tile size), so a reuse-based model would misrepresent a benefit this book's own generated code cannot realize. 22.1 (File 054): `estimateLoopOverheadCost()` -- a simple linear model (trips through a loop times a fixed per-trip overhead) applying identically to tile size and unroll factor, per Chapter 21's own finding that they're the same underlying question; tested against a real 50-million-element `computeUnrolled()` run, best-of-5 timed via `std::chrono`, on both machines. The model's own formula is monotonic (always predicts the largest chunk size as cheapest); real measurement found a genuine turnover point the model cannot see, and that turnover point landed at a DIFFERENT chunk size on each real machine (chunkSize=32 on the cloud sandbox x86-64, chunkSize=8 on the aarch64 device) -- both real, honestly reported, machine-specific findings, not a discrepancy to reconcile. 22.2 (File 055): `estimateLoopOrderCost()` -- a model built on counting cache-line loads (16 floats per 64-byte line) for a schedule's own loop order, tested on a real 4096x4096 (64 MB) array traversal, row-major vs. column-major. The model correctly predicts row-major cheaper (an 11.7x line-load-diluted ratio) and real measurement confirms the DIRECTION by a dramatic margin on both machines (28-50x, varying by machine) while the model's own predicted MAGNITUDE undershoots reality on both -- diagnosed as real hardware prefetching and TLB effects the model never included. 22.3 (File 056, capstone): `estimateScheduleCost()` sums both prior models unchanged (real per-element work counted ONCE, not per mechanism) and scores 6 schedules evenly drawn from Chapter 21's own 128-schedule list, executed for real through a generic, runtime-parameterized schedule INTERPRETER (`executeSchedule()`, reading a `Schedule`'s own fields as data rather than one hand-written function per candidate) run 800,000 times per schedule (best-of-7) since the toy `LoopNest`'s 48 total elements are too small to register on a wall clock otherwise. Combined-model ranking agreement landed close to chance on both machines (40% cloud, 53% device, out of 15 pairwise comparisons) -- honestly diagnosed as interpreter bookkeeping overhead dominating a 48-element problem (not proof the underlying models are worthless), supported directly by Section 22.2's own much stronger real-compiled-loop result. 3 files, plain C++ (no CUDA linkage) -- correctness (bit-for-bit self-checks) cross-verified identically on both the cloud sandbox and the device; TIMING figures deliberately reported as real, machine-specific, and NOT required to match cross-machine, a first for this book's own verification discipline (every prior chapter through 21 required exact cross-machine agreement, since none of that work depended on the clock). Diagram-heavy per the Ch5-21 depth standard (4 hand-drawn diagrams: opening 3-mechanism overview + one per section); zero diagram-checker violations after one fix (a stray `<-` arrow caught and corrected). No new external sources cited -- both cost models and their combination are original to this book, building on Chapter 15's own `LoopNest`/`tileLoop()` machinery and Chapter 21's own `Schedule`/`enumerateSchedules()` machinery; the cache-line/memory-stride/prefetching background Section 22.2 demonstrates is well-documented general knowledge about real memory hierarchies, verified here by this book's own directly executed and measured example on two different real machines. TOC's Chapter 21 entry above already carried its real hash (c6d3ef6, fixed by the follow-up commit 26aa168) when this entry was added -- nothing stale to fix here.

NEXT: Chapter 23, "Building CUDA Hammer's Autotuner" (Part 5 closes) -- builds the actual SEARCH Chapter 22's own cost models were built to guide: given a real `LoopNest`, its own real enumerated schedule space (Chapter 21), and a cost model honestly known to be imperfect in specific, diagnosed ways (Chapter 22), how should an autotuner actually decide which candidates to try? The likely design, set up directly by Chapter 22's own closing finding, is a HYBRID: use the cost model to prune a large space down to a short list cheaply, then pay for real, compiled measurement (Part 4's own codegen, not Chapter 22's own interpreter) only on that short list. Apply the Chapter 5-22 depth-level standard.
