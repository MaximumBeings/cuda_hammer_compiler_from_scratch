# CUDA Hammer: A Tensor Compiler in C++ -- Table of Contents (planning document)

Subtitle: From a Minimal IR to Fused, Autotuned Kernels

This is the full planned outline. Check this file first before starting any new chapter or appendix. Mirrors the sibling books' own build-verify-lock discipline (see getting-started.md for this book's own specific honesty discipline around the toolchain actually available while writing it).

## Part 0 -- Why Compile Tensor Programs At All
1. The Cost of Eager Execution
2. What a Compiler Pipeline Looks Like
3. A Tour of Real ML Compilers: XLA, TVM, and Triton

## Part 1 -- CUDA Hammer's IR: A Minimal Tensor Graph From Scratch
4. Representing Tensors and Operations as a Graph
5. Building the Graph: CUDA Hammer's Frontend
6. Graph Validation and Shape Inference
7. Printing and Visualizing CUDA Hammer's IR

## Part 2 -- Basic Optimization Passes
8. The Pass Manager: Structuring Compiler Transformations
9. Constant Folding and Dead Code Elimination
10. Common Subexpression Elimination for Tensor Graphs
11. Algebraic Simplification

## Part 3 -- Operator Fusion
12. Why Fusion Matters: Memory-Bound vs. Compute-Bound Kernels
13. Elementwise Fusion
14. Fusing Reductions
15. Loop Fusion and Tiling for Fused Kernels
16. Fusion Boundaries: What Can't Be Fused, and Why

## Part 4 -- Code Generation
17. Lowering CUDA Hammer's IR to Loops
18. Generating CUDA C++ From the Fused IR
19. Generating Vectorized CPU Code
20. A JIT Backend: Compiling and Loading Generated Code at Runtime

## Part 5 -- Autotuning
21. The Search Space: Tile Sizes, Loop Orders, and Unrolling
22. Cost Models vs. Measurement-Based Autotuning
23. Building CUDA Hammer's Autotuner
24. Caching and Reusing Tuned Schedules

## Part 6 -- Case Studies: How CUDA Hammer Compares to Real Compilers
25. XLA's HLO and Fusion Passes
26. TVM's Relay/TIR and Ansor Auto-Scheduling
27. Triton's Block-Level Programming Model
28. torch.compile and TorchInductor
29. Flash Attention as a Fusion Case Study
30. Quantization-Aware Codegen

## Appendices
- A. Installation and Setup -- Building CUDA Hammer's Toolchain
- B. Practice Quiz
- C. LLVM and MLIR: The Compiler Infrastructure You Get for Free
- D. Polyhedral Compilation and Advanced Loop Transformations
- E. Profiling and Benchmarking Generated Kernels
- F. From torch.compile and TVM's Python API to C++: A Rosetta Stone
- G. Common Failure Modes: Miscompilation, Fusion-Order Drift, and Autotuner Overfitting

## Status
Scaffolded 2026-09-21: mkdocs.yml, docs/index.md, docs/getting-started.md, this file.
Toolchain confirmed this session: cloud sandbox is x86_64, g++ 13.3.0, clang++ 18.1.3, real apt-installed nvcc 12.0 (genuine `nvcc`/`ptxas`/`cicc`, not the incomplete pip wheel the sibling books already documented), AVX2/AVX512F/FMA present in `/proc/cpuinfo`. Device (connected folder, isolated Linux VM on the user's Mac) is aarch64, g++ 11.4.0, no clang++, no nvcc -- but genuine NEON (`asimd`) confirmed by actually compiling and running an `arm_neon.h` test program there (output `3.0 3.0 3.0 3.0`, correct). This means Part 4's CPU-codegen chapter (Ch19) can genuinely compile AND RUN real vectorized kernels on two real, different architectures (AVX2/FMA on the cloud sandbox, NEON on the device) with zero simulation needed for that backend -- unlike the CUDA backend (Ch18), which compiles for a real arch via nvcc but cannot execute anywhere in this toolchain (no physical GPU on either side), the same honest limitation the sibling CUDA books already carry.
Chapter 1 (Part 0, DONE, commit `b1f1933`): "The Cost of Eager Execution" -- opens the book. Two real, independent costs of eager execution quantified without any fabricated timing: 1.1/1.3 a genuinely counted memory-traffic model (eager(N) = N * 2 * array_bytes vs. fused = 2 * array_bytes, checked for N=1..8, EAGER and FUSED outputs verified bit-identical); 1.2 NVIDIA's own real cited kernel-launch-overhead figure (9.6us per kernel, "Getting Started with CUDA Graphs" blog post) applied via a real formula to the same chain lengths, plus a closing synthesis on a concrete 4-op/1M-element example that deliberately reports the two reductions SEPARATELY rather than fabricating a combined wall-clock speedup. 2 files (both plain C++, no CUDA/NCCL/MPI linkage -- cross-verified on both cloud sandbox and device), 4 ASCII diagrams (chapter-opening + one per section), zero diagram-rule violations on first check.
[stated] Planning note (2026-09-22, not yet placed in the chapter/appendix plan): user asked to integrate NVIDIA CUTLASS (the real production GEMM/tensor-op template library) at some point. Natural homes to decide between when we get there: a new Part 6 case-study chapter ("How CUTLASS Generates Production GEMM Code"), or folding it into Appendix C alongside LLVM/MLIR as a second "infrastructure you get for free" example. Not yet assigned a chapter number -- revisit before Part 4 (codegen) or Part 6 (case studies) so numbering doesn't need to be redone later.
[stated] Theme palette changed 2026-09-22: yellow/amber (was deep purple/purple).
Chapter 2 (Part 0, DONE, commit `c39c0bd`): "What a Compiler Pipeline Looks Like" -- completes Part 0's own build-up before Part 1 starts CUDA Hammer itself. Builds general compiler vocabulary (lex, parse, AST, lower, linear IR, pass, codegen) on a genuinely implemented tiny arithmetic-expression compiler, not tensors yet. 2.1/2.2 (File 003): real lexer + recursive-descent parser + AST + AST-to-three-address-code lowering + IR interpreter, cross-checked against an independent direct-AST evaluator across 5 expressions (precedence and left-associativity both genuinely exercised, e.g. `a - b - c`). 2.3 (File 004, reuses File 003's front end unchanged): a real constant-folding + constant-propagation pass operating purely on the IR, genuinely eliminating up to 100% of an expression's instructions (one test folds to zero instructions), correctness checked by interpreting before and after folding on every expression (all matched), 66.7% total instruction reduction across the 5 test expressions. 2 files, both plain C++ (no CUDA linkage), zero diagram-rule violations across 4 diagrams on first check. No external sources cited -- general compiler-construction vocabulary from first principles.
Chapter 3 (Part 0, DONE, commit `935b6a6`): "A Tour of Real ML Compilers: XLA, TVM, and Triton" -- closes Part 0. A citation-heavy survey chapter, not a code chapter (its own `code/` directory is intentionally empty, per Getting Started's own honesty discipline: claims about other projects are cited to their own real documentation, not measured by this book). Maps Chapter 2's vocabulary (IR, pass, lowering, codegen) onto three real production ML compilers: 3.1 XLA's HLO IR and its two-phase (target-independent then backend-specific) fusion passes, cited to openxla.org's own architecture/hlo_passes/gpu_architecture pages, including the direct quote "Fusion is XLA's single most important optimization"; 3.2 TVM's TensorIR and its explicit schedule primitives (`split`/`tile`/`reorder`/`bind`/`fuse`), cited to tvm.apache.org's own TensorIR and schedule-primitives docs, plus the DLight (rule-based) vs. MetaSchedule (search-based) distinction that foreshadows CUDA Hammer's own Part 5; 3.3 Triton's block-level "Blocked Program, Scalar Threads" model (inverted from CUDA's own "Scalar Program, Blocked Threads"), cited to triton-lang.org's own programming-guide introduction. 6 real sources cited, all fetched and quoted directly from each project's own current documentation this session. 4 ASCII diagrams, one stray `<-` caught and fixed by the automated diagram check before locking (replaced with `--`), zero violations after the fix.
[stated] Header bar text/icons changed to red 2026-09-23 (commit `9989464`), via `docs/stylesheets/extra.css` (`#c1121f`, wired in through `mkdocs.yml`'s `extra_css`) -- scoped to the header bar only, per the user's own explicit scope choice.
Chapter 4 (Part 1, DONE, commit `3170666`): "Representing Tensors and Operations as a Graph" -- opens Part 1, CUDA Hammer's first real code chapter (no more parsing arithmetic text -- direct graph-construction API calls from here forward). 4.1/4.2 (File 005): real `Value`/`Node`/`Graph` classes; a genuinely shared 6-node diamond DAG built only through `addInput`/`addBinary`/`addUnary` (never parsed from text), with a computed non-leaf node (`t1`) proven to have exactly 2 real consumers, plus a deliberately unmemoized recursive `expandToTreeNodeCount()` genuinely computing that an equivalent unshared tree would need 10 nodes instead of 6 -- a real counted IR-size cost of not sharing, echoing Chapter 1's own memory-traffic argument. 4.3 (File 006, reuses File 005's classes unchanged): a real topological sort (Kahn's algorithm), cross-checked by an independent positional validator, confirmed valid on the normal DAG; the same checker correctly detects a cycle in a second graph deliberately hand-broken by reaching past the public API (`mutableNode()`) to mutate an existing node's inputs after construction -- proving the normal API's cycle-freedom-by-construction guarantee by breaking it on purpose. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 6 ASCII diagrams total (4 hand-drawn + 2 locked verbatim program-output blocks, the latter correctly exempt from the diagram-character rule since they contain real printf output using `<-`), zero violations on first check. No external sources cited -- CUDA Hammer's own graph design is original to this book.
[stated] Starting with Chapter 5, chapters get MORE prose explanation and MORE ASCII diagrams before each code block, per the user's own explicit request mid-Chapter-5 -- same depth-first-then-code discipline already standing for the CUDA C++ DSA book, now applied here too.
Chapter 5 (Part 1, DONE, commit `12ac633`): "Building the Graph: CUDA Hammer's Frontend" -- Part 1 continues. A minimal, flat text format ("name = op(arg, ...)", every right-hand side a call, no exceptions) parsed straight into a Graph with NO AST stage -- a real architecture choice (the same one LLVM's own real IR text format uses), not a shortcut, since this grammar is flat rather than recursive. 5.1/5.2 (File 007): a tokenizer (6 token kinds) and a one-pass `parseStatement()`/`parseProgram()` calling straight into Chapter 4's own `addInput`/`addConst`/`addUnary`/`addBinary`, resolving every identifier argument through a `SymbolTable` the instant it's read. Correctness proven the strong way: parsing the Chapter 4 diamond program produces a graph `graphsStructurallyEqual()` confirms is node-for-node, edge-for-edge IDENTICAL to the graph Chapter 4 built by hand. 5.3 (File 008, reuses File 007's parser unchanged): four deliberately invalid programs (undefined name, redefinition, arity mismatch, unknown operation), each isolated to its own specific check inside `parseStatement()` and confirmed to throw a `std::runtime_error` naming the real problem, plus a fifth valid control program confirming none of those checks misfire on correct input. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Per the user's own mid-chapter request, this chapter carries substantially more prose (a full walk of the symbol table's state statement-by-statement, a token-by-token trace of one line, a table mapping each of the 4 error cases to the exact check that catches it) and more diagrams (9 total, up from the prior chapters' usual 4-6) than Chapters 1-4 -- this depth level now applies to Chapter 6 onward too. No external sources cited.
Chapter 6 (Part 1, DONE, commit `d26cdca`): "Graph Validation and Shape Inference" -- closes Part 1. Shape lives as a separate, computed side table (`map<nodeId, Shape>`), never a field on `Node` -- a deliberate pass/IR separation, not an oversight. 6.1/6.2 (File 009): the real NumPy-style broadcasting rule (cited to numpy.org's own docs, quoted directly), tested standalone on 5 hand-verified cases; `inferShapes()` reuses Chapter 4's own `topologicalSort()` completely unchanged to walk a graph in a guaranteed-valid order, computing each node's shape from its already-known inputs (leaves from a caller-supplied declaration, Add/Mul via broadcasting, ReLU shape-preserving) -- tested end to end on the Chapter 4 diamond graph, correctly inferring [3,4] through two broadcasts and a shape-preserving ReLU. 6.3 (File 010, reuses File 009 unchanged): 3 deliberately invalid graphs (2 flavors of incompatible shapes, 1 missing declared leaf shape), each isolated to the exact check that catches it, plus a valid control case. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 9 ASCII diagrams (continuing the Chapter 5 depth level), one stray `<-` in the opening diagram caught and fixed before locking (replaced with `, inputs`), zero violations after. 1 real external source cited (NumPy's own broadcasting rule).
Chapter 7 (Part 1, DONE, commit `395ccda`): "Printing and Visualizing CUDA Hammer's IR" -- closes Part 1. Two genuinely different external views of the same Graph. 7.1/7.2 (File 011): `printGraphAsSource()` emits Chapter 5's own exact "name = op(args)" grammar (using each input's debugName, never a numeric id) and is checked the strong way -- parseProgram(printGraphAsSource(g)) round-trips to a graph graphsStructurallyEqual() confirms is IDENTICAL to the original, both for the Chapter 4 diamond graph and for a second graph exercising the one special case (Const, whose argument is a number, not a name). Shows node-creation order is already a valid print order (same cycle-freedom-by-construction argument as Chapter 4), needing no topologicalSort() call. 7.3 (File 012, reuses Ch4 Graph/topologicalSort() and Ch6 Shape/inferShapes() unchanged): `exportToDot()` emits real Graphviz DOT (grammar cited directly to graphviz.org's own docs, quoted), with each node's label showing its Chapter-6-inferred shape too. Validated two ways: code-level structural counts (node count, edge count, out's in-degree), AND -- the strong claim -- graphviz actually installed in the cloud sandbox this session (`apt-get install graphviz`) and the generated file handed to the real `dot` binary, confirmed accepted (exit 0) by Graphviz's own parser, not just asserted to look like valid DOT. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device (Graphviz-binary validation step in File 012 gracefully skips if `dot` isn't installed, e.g. on the device, without failing the build). 9 ASCII diagrams (continuing the Ch5/Ch6 depth level); 3 diagrams initially flagged by the automated checker (a "<--"-annotated round-trip loop, several "<-" substitution annotations, a "<" less-than in prose, a "v" used as a downward arrow) and rewritten before locking -- zero violations after. 1 real external source cited (Graphviz's own DOT language grammar).
Chapter 8 (Part 2 opens, DONE, commit 7ef4fe6): "The Pass Manager: Structuring Compiler Transformations" -- Part 2 begins. Formalizes the analysis-pass/transform-pass distinction using Ch6's inferShapes() and Ch7's printGraphAsSource() as the analysis-pass examples already in hand. 8.1/8.2 (File 013): TransformPass = std::function<Graph(const Graph&)> -- a real design consequence of Graph having no node-removal API (motivated explicitly for Ch9's upcoming DCE); canonicalizeNodeNames(), the book's first real transform pass, renumbers every node's debugName to "%<id>" in topological order, checked the strong way -- graphsStructurallyEqual() (which never looks at debugName) confirms the computation is unchanged while names demonstrably changed, plus a check that the ORIGINAL graph's own names are untouched (proving the pass is read-only on its input, as the TransformPass contract requires). 8.3 (File 014, reuses File 013's types/passes unchanged): a real PassManager (`runPasses()`) threading a graph through a sequence of named passes, printing a before/after dump of EVERY pass via Ch7's own printGraphAsSource() (the first real use of that printer outside its own chapter) and checking DAG validity via Ch4's topologicalSort() after every single pass, not just at the end. Tested on a well-behaved 2-pass pipeline ([identityPass, canonicalizeNodeNames]) AND, the book's own every-chapter negative case, a deliberately broken pass (breakGraphByInsertingCycle(), reaching past the API via mutableNode() the same way Ch4's File 006 did) that runPasses() catches immediately, naming the exact pass responsible. 2 files, both plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. 9 ASCII diagrams (continuing the Ch5/6/7 depth level); 2 diagrams initially flagged by the automated checker (a "<" inside "map<int, Shape>" and inside a std::function<...> type signature, both rewritten in prose/words rather than literal angle-bracket syntax) and fixed before locking -- zero violations after (the one remaining "<" the checker flags is inside a locked verbatim Output block, exempt). No external sources cited -- pass/PassManager design is original to this book, built from vocabulary Chapters 2/4/5/6/7 already established; the general analysis-vs-transform-pass distinction is treated as general architectural knowledge (LLVM's own pass infrastructure is one well-known example), consistent with how Chapter 5 treated its own LLVM comparison.
Chapter 9 (DONE, commit 08f2414): "Constant Folding and Dead Code Elimination" -- the first genuinely optimizing passes, plugging directly into Chapter 8's own TransformPass/PassManager infrastructure with zero changes to that infrastructure. 9.1 (File 015): constantFoldPass() walks topologicalSort() order and folds Add/Mul/ReLU nodes into Const nodes by checking the NEW graph being rebuilt (not the old one), which lets an entire constant chain -- c1+c2, then that result *c3 -- fold in a single pass rather than needing to run to a fixed point; a new evaluate() interpreter is introduced alongside it, keyed by node NAME rather than id after a real bug (a genuine mid-session crash: the folded graph's topological order shifted `x` to a different id than it had originally, because c1/c2/c3/x are all simultaneously-ready leaves under Kahn's algorithm -- turned into the chapter's own [COMMON TRAP] admonition using the actual printed before/after output as proof, rather than silently fixed and hidden). 9.2 (File 016): deadCodeEliminationPass() -- the first real payoff of Chapter 8's "rebuild rather than mutate" design, stated as the reason that design exists. Introduces the explicit, honestly-stated convention that the LAST node in a graph is its own designated output (needed because Graph itself has no notion of "output" at all), computes backward liveness from that root via computeLiveNodeIds() walking node->inputs against topologicalSort()'s own forward direction, and rebuilds keeping only live nodes. Tested on a dead node built from real live inputs (not an obvious orphan), a node with two live consumers (kept exactly once, both consumers correctly remapped), and a control case (an already-clean graph comes back unchanged). 9.3 (File 017, reuses Files 015+016 verbatim plus Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass): runs [constantFoldPass, deadCodeEliminationPass] back to back through Chapter 8's own unmodified PassManager on a 7-node graph -- folding alone leaves node count unchanged (7->7, it only replaces computations, never removes nodes), DCE afterward drops it to 3 nodes, and a real code check confirms DCE run ALONE on the unfolded original graph removes nothing (7->7) -- substantiating "order matters" with an actual measurement rather than an assertion. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5/6/7/8 depth standard; zero ASCII-diagram-checker violations. No external sources cited -- constant folding and DCE are original design here, explicitly tied back to Chapter 2's own File 004 constant-folding pass (a different, simpler toy IR) as internal precedent rather than an external citation.
Chapter 10 (DONE, commit 29ebf41): "Common Subexpression Elimination for Tensor Graphs" -- Part 2's third optimizing pass, again plugging into Chapter 8's unmodified TransformPass/PassManager infrastructure with zero changes. 10.1 (File 018): commonSubexpressionEliminationPass() computes a canonical cseKey() for each node (an Input's own name; a Const's literal value; an Add/Mul/ReLU's op plus the NEW, already-rebuilt ids of its operands) and, walking topologicalSort() order, skips re-adding any node whose key already exists, remapping its consumers to the earlier node instead -- the same "just don't add it" idiom Ch9's DCE used, here because something IDENTICAL already exists rather than because nothing needs it. Because the key reads the NEW graph (oldIdToNewValue), a merge discovered early in the walk cascades into further merges discovered later in the SAME pass (demonstrated twice: a duplicate Add node test, and a duplicate-Const test where two independently-created Const(7) nodes merge and that merge cascades into merging the two Add nodes that consumed them, 6->4 nodes). Also includes a control case (no duplicates -> unchanged). 10.2 (File 019): characterizes cseKey()'s real boundary with actual executed evidence on BOTH sides -- add(a,b) and add(b,a) are mathematically identical but get DIFFERENT keys (operand order is encoded, not commutativity-aware) and are deliberately NOT merged, a real, honestly-stated conservative limitation (staying conservative avoids the much worse alternative: merging operands for an operation that ISN'T actually commutative, a silent correctness bug); contrasted against two separately-created Input nodes sharing a name, which DO correctly merge (cascading into their own consumers too), since Ch9's own evaluate() looks an input up purely by name, making that merge genuinely safe rather than a missed shortcut. 10.3 (File 020, reuses Ch9's constantFoldPass()+deadCodeEliminationPass()+evaluate() and Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass verbatim): builds a graph where an Add node ("p" = c1+c2) will fold to the exact same VALUE as an independently-created Const ("c3" = 5), but CSE alone cannot see this before folding runs (an Add node and a Const node have different cseKey() prefixes regardless of eventual value) -- proven by actually running CSE ALONE on the unfolded original graph first and confirming it finds nothing (8->8), the same "prove it, don't just assert it" discipline Ch9's 9.3 used for its own order-matters claim. The full [constantFoldPass, commonSubexpressionEliminationPass, deadCodeEliminationPass] pipeline then reduces the graph 8->8(fold)->6(CSE)->4(DCE) nodes, with the CSE merge itself creating NEW dead code (c1, c2, no longer referenced once "p" is elided in favor of "c3") that only the final DCE pass cleans up -- a fresh, three-pass-spanning instance of the exact "could running this again find more" question Ch9's own [COMMON TRAP] left open, extended from a new angle (folding creating a CSE opportunity, not just DCE cleanup) rather than resolved. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5-9 depth standard; zero ASCII-diagram-checker violations. No external sources cited -- CSE is standard, decades-old compiler-construction vocabulary, not specific to any one real compiler needing citation, same treatment Ch8 gave the general analysis-vs-transform-pass distinction.
Chapter 11 (DONE, commit 34406ca): "Algebraic Simplification" -- Part 2's fourth and closing optimizing pass, again plugging into Chapter 8's unmodified TransformPass/PassManager infrastructure with zero changes. 11.1 (File 021): algebraicSimplificationPass() is structurally the twin of Ch9's constantFoldPass() -- it walks topologicalSort() order and rebuilds via Ch4's addInput/addConst/addUnary/addBinary, but at an Add/Mul node it checks the NEW, already-rebuilt operands against arithmetic identities (additive identity x+0=x, 0+x=x) and, on a match, points the new Value directly at the ALREADY-EXISTING operand -- no new node built at all, not even a Const, a genuinely different "shape" of match than either constantFoldPass()'s new-Const-answer or CSE's reuse-an-earlier-duplicate. Demonstrated on a graph where add(x,0) and add(0,x) both elide to plain x while a control add(x,y) survives untouched, confirmed both structurally (elided names gone, survivor's both operands literally the same node) and by evaluate() agreement before/after. 11.2 (File 022, reuses File 021's pass verbatim): extends the identity table with the multiplicative identity (x*1=x, 1*x=x), the multiplicative absorbing element (x*0=0, 0*x=0 -- which DISCARDS the other operand entirely, a genuinely different shape of identity than the other three, since it throws away not just the redundant identity operand but the whole other side, along with anything IT depended on), and ReLU idempotence (relu(relu(x))=relu(x), checked by asking whether a ReLU's own already-rebuilt operand is itself a ReLU node). A deliberately-built 16-node test graph demonstrates a 3-level cascade discovered in a single pass -- two independent mul(_, 0)s both elide to the SAME zero constant, an Add combining those two now-identical zeros elides AGAIN to that same zero, and an Add combining that zero with something else elides a THIRD time to that something else -- 16->9 nodes, all elided names gone, every survivor correctly rewired, evaluate() agreement preserved. 11.3 (File 023, reuses Ch9's constantFoldPass()+deadCodeEliminationPass(), Ch10's commonSubexpressionEliminationPass(), and this chapter's own algebraicSimplificationPass() verbatim, plus Ch7's printGraphAsSource() and Ch8's runPasses()/NamedPass): closes Part 2 with all four of its passes running together through Chapter 8's still-completely-unmodified PassManager. Part A proves algebraicSimplificationPass() ALONE, run first, finds nothing on a graph whose simplification opportunity only exists after folding (13->13, the same "prove it, don't assert it" discipline every prior chapter in Part 2 has used for its own order-matters claim). Part B runs the full [constantFoldPass, commonSubexpressionEliminationPass, algebraicSimplificationPass, deadCodeEliminationPass] pipeline end to end: 13(original)->13(fold)->12(CSE merges a literal duplicate Add)->10(simplification elides two more nodes into aliases of earlier ones)->4(DCE sweeps up everything simplification's aliasing left newly dead), collapsing to the algebraic minimum add(x1,x2) squared, evaluate() agreement confirmed at 49 before and after. Part C is a small, deliberate, separate demonstration of a real hazard this chapter's own pass can trigger: when algebraicSimplificationPass() elides the LITERAL last node a graph ever added (here, a 3-node out=add(x,zero) where the whole graph elides to just x), Chapter 9's own "the last node is the graph's own designated output" convention -- which deadCodeEliminationPass() depends on directly -- silently ends up pointing at the WRONG node afterward (here, 'zero', not the true output), proven with real, minimal, executable code rather than asserted; framed explicitly as a concrete instance of a limitation Chapter 9 had already stated in the abstract (Worked Solution #4), with no fix offered -- an open question carried forward, the same honesty discipline every "could running this again find more" moment in Part 2 has followed. 3 files, all plain C++ (no CUDA linkage) -- cross-verified on cloud sandbox and device. Diagram-heavy per the Ch5-10 depth standard; zero ASCII-diagram-checker violations after one stray "<-" (caught by the automated checker) was fixed. No external sources cited -- algebraic identities are general mathematical facts, not specific to any one real compiler needing citation.

Part 2 (Chapters 8-11) is now COMPLETE: The Pass Manager, Constant Folding and Dead Code Elimination, Common Subexpression Elimination, and Algebraic Simplification -- four TransformPasses, all built on Chapter 8's own unmodified PassManager/TransformPass infrastructure with zero changes made to that infrastructure across the whole of Part 2.

Chapter 12 (DONE, commit bceecc0): "Why Fusion Matters: Memory-Bound vs. Compute-Bound Kernels" -- opens Part 3, playing the same role for operator fusion that Chapter 1 played for the whole book: establishing, with real counted and cited evidence, exactly what the next several chapters (13-16) are trying to save. 12.1 (File 024): arithmetic intensity (totalFLOPs / totalBytesMoved) computed as a closed-form ratio for a chain of K unary elementwise ops -- AI_unfused = 1/(2*bytesPerElement), a CONSTANT independent of K (the K cancels completely, since traffic and work both scale by K in lockstep), while AI_fused = K/(2*bytesPerElement), GROWING linearly with K (fused traffic never grows past one read + one write, but fused work keeps growing) -- explicitly framed as the quantitative gap Chapter 1's own Worked Solution #7 already named as missing. 12.2 (File 025, reuses 024's two AI functions verbatim): the roofline model, real published work (Williams, Waterman, Patterson, CACM April 2009), with a real ridge point (peakFLOPs/peakBandwidth) computed from NVIDIA's own cited A100 datasheet (19.5 TFLOPS FP32, 1,555 GB/s) -- 12.5402 FLOPs/byte -- proving by direct computation that an UNFUSED chain is memory-bound at every tested K including 1,000,000 (its AI never moves), while a FUSED chain crosses into compute-bound at a real computed threshold, K=101, with the boundary (K=100 not compute-bound, K=101 is) confirmed exactly. 12.3 (File 026, reuses Ch4's Value/Node/Graph/topologicalSort() and Ch6's Shape/broadcastShapes()/inferShapes() completely unchanged, plus 025's ridge-point constants): moves from a hand-picked chain to a REAL Graph -- Chapter 4's own diamond (a,b inputs; t1=Add(a,b) with TWO consumers t2 and t3; out=Add(t2,t3)) -- counting unfused bytes (every non-leaf node's own kernel re-reads shared operands like 't1' once per consumer) vs. fused bytes (each distinct leaf read once via Ch9's "last node is the output" convention, output written once) directly from the graph's own Ch6-inferred shapes rather than a formula; found a real 4.4x AI improvement (0.096774 -> 0.428571 FLOPs/byte) even though both land in the same memory-bound classification at this toy graph's tiny scale (12 elements/node), honestly framed as evidence the ratio improvement is real at any scale while crossing the actual ridge point needs the far larger tensors Part 3's real fusion passes will operate on. 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-11 depth standard; one roofline ASCII diagram's stray "<-" annotations caught by the automated checker and fixed (replaced with "--"), zero violations after. 3 real external sources cited and fetched fresh this session: Williams/Waterman/Patterson's original 2009 CACM roofline paper (concept, not directly quoted -- full text is paywalled), NERSC's own roofline documentation (quoted directly for its arithmetic-intensity definition and "machine balance point" naming of the ridge point), and NVIDIA's own A100 datasheet PDF (fetched directly, FP32 TFLOPS and memory bandwidth figures verified before use). mkdocs build --strict clean; nav/TOC updated (new "Part 3 -- Operator Fusion" heading added); TOC's stale Chapter 11 "commit pending" placeholder also fixed in this same edit (self-correction pattern holding steady: Ch10->Ch11, Ch11->Ch12 -- keep checking at Ch13) -- push+build handoff given to user after this entry.

Chapter 13 (DONE, commit pending): "Elementwise Fusion" -- Part 3's first real fusion pass, and the first genuinely new IR node kind since Chapter 4: OpKind::FusedElementwise, whose body is a small ordered internal program (std::vector<FusedStep>, each step an Add/Mul/ReLU reading either an ExternalInput -- the node's own inputs list, i.e. memory, read once -- or a PriorStep -- an earlier step's result within the SAME node's program, i.e. a register, never memory). 13.1 (File 027): the new IR itself (Value/OpKind/OperandKind/FusedOperand/FusedStep/extended Node/extended Graph with addFusedElementwise(), a new ad hoc printGraph() debug printer since Ch7's printGraphAsSource() has no grammar for a node whose body is a program, and evaluate() extended to execute a fused node's internal steps into a local register array) plus elementwiseFusionPass() itself: walks topologicalSort() order: Input/Const rebuild trivially; single-consumer Add/Mul/ReLU are skipped (inlined later by their one consumer); everything else is materialized by resolveIntoGroup(), a recursive helper that inlines any single-consumer elementwise operand as a new FusedStep (recursing into ITS operands first) or treats it as an ExternalInput otherwise, deduplicating repeated references within one fused body via two id->index maps so a value read twice costs one memory read, not two; a 1-step result emits a plain node (no trivial wrapper), only >1 steps emit a real FusedElementwise node. Demonstrated on a straight single-consumer chain (mul->add->relu, all single-consumer): 6 nodes -> 4, one FusedElementwise node with 3 external inputs and 3 steps, evaluate() agreement confirmed at two different inputs. Honestly notes as a stated limitation (not fixed) that a value used TWICE by the SAME consumer (e.g. mul(t1,t1)) is outside this pass's own single-consumer-count rule as written. 13.2 (File 028, reuses File 027's entire type/pass machinery verbatim): proves a fusion boundary is forced EXACTLY when a node has more than one consumer, by hand-deriving the expected structure BEFORE running the code and then confirming an exact match, on Chapter 4's own diamond graph (t1=Add(a,b) has 2 consumers t2,t3 -- stays materialized as its own plain node -- while t2/t3/out, each single-consumer, fuse into one FusedElementwise node: external inputs [t1,a], steps [Mul,ReLU,Add], 6 nodes -> 4) versus a deliberately modified control graph with that one shared value removed (fuses completely into a single node, 4 nodes -> 3) -- same pass, unmodified, two different real outcomes because the GRAPH's own structure differs, not the pass. evaluate() agreement confirmed on both graphs. 13.3 (File 029, reuses 027/028's machinery plus Ch6's Shape/broadcastShapes()/inferShapes() and Ch8's TransformPass/NamedPass/runPasses() all unchanged, via a one-line elementwiseFusionTransform() wrapper since the pass's own richer FusionResult -- which also carries representativeOldId, a new-node-id->original-node-id map used as a side channel to reuse Ch6's shape inference across the pass without teaching it about the new node kind -- doesn't fit Chapter 8's plain TransformPass signature): the fifth real optimization pass (after constant folding, dead code elimination, CSE, and algebraic simplification) to plug into Chapter 8's PassManager unmodified, measured on Chapter 4's own diamond graph against BOTH of Chapter 12's own bounds -- bytes moved before fusion: 496 (exactly matching Chapter 12's own Section 12.3 figure), after fusion: 256, landing strictly between Chapter 12's idealized single-kernel bound (112, computed by a formula that never had to check real data sharing) and the fully-unfused figure (496) -- honestly framed as the real, achievable result once genuine sharing constraints (t1's 2 consumers) are respected, not a shortfall to explain away. AI improves 0.096774 -> 0.187500 FLOPs/byte (real, though still memory-bound at this toy graph's tiny scale, consistent with Chapter 12's own scale caveat). evaluate() agreement confirmed before/after fusion. Caught and fixed a real compile error during writing: an initial runPasses() draft used copy-assignment (Graph current = initial; ... current = next;), which fails because Graph owns vector<unique_ptr<Node>> and is deliberately non-copyable -- fixed by matching Chapter 8's own File 014 pattern exactly (first pass reads initial by const reference, every later pass moves its result into current via std::move). 3 files, all plain C++ (no CUDA linkage) -- cross-verified byte-identical + output-identical on cloud sandbox and device. Diagram-heavy per the Ch5-12 depth standard; one diamond-fusion ASCII diagram's stray "<-" annotations and one "112 < 256 < 496" stray less-than comparison caught by the automated checker and fixed (replaced with "--"), zero violations after. No new external sources cited -- the FusedElementwise design is explicitly modeled on XLA's own real fusion computations, already cited in Chapter 3. TOC's stale Chapter 12 "commit pending" placeholder also fixed in this same edit (self-correction pattern holding steady: Ch11->Ch12, Ch12->Ch13 -- keep checking at Ch14) -- push+build handoff given to user after this entry.

NEXT: Chapter 14, "Reduction Fusion" (Part 3 continues) -- extends elementwiseFusionPass()'s own fusion machinery to handle a genuinely different shape of operator: a reduction (e.g. sum) that collapses a tensor's own shape rather than preserving it elementwise, forcing the pass to confront which fusions are still safe when the fused body's own output shape no longer matches its inputs' shapes. Apply the Chapter 5-13 depth-level standard (more prose, more diagrams before code) here too.
